---
phase: 16-weekly-kpi-selection-scorecard-counters
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - WEEKLY-06
must_haves:
  truths:
    - "REQUIREMENTS.md WEEKLY-06 text reflects D-02 override: lock at commit, only Trace can change, not at scorecard submit"
    - "Old WEEKLY-06 string mentioning 'until the scorecard for that week is submitted' no longer present"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected WEEKLY-06 acceptance criterion"
      contains: "after commit, only Trace"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 16 planner/executor decisions"
      via: "WEEKLY-06 canonical text alignment with D-02"
      pattern: "WEEKLY-06"
---

<objective>
Apply the surgical edit to REQUIREMENTS.md WEEKLY-06 to memorialize the D-02 user override: selection locks at CONFIRM (not at scorecard submit), and only Trace (admin, Phase 17) can change mid-week. This is the same override pattern as Phase 15 D-20/D-21 GROWTH-02.

Purpose: Prevent future agents from re-implementing the pre-override semantic by reading a stale requirement. Pitfall 4 from RESEARCH.md.
Output: One surgical bullet-point edit in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/16-weekly-kpi-selection-scorecard-counters/16-CONTEXT.md

<interfaces>
Current WEEKLY-06 text (line 52 of REQUIREMENTS.md):
"Partner can change the weekly choice until the scorecard for that week is submitted; after submission, selection is locked"

D-02 new text (from CONTEXT.md):
"Partner commits via confirmation; after commit, only Trace can change the selection until the week naturally rolls over."

Precedent pattern: Phase 15 D-20/D-21 GROWTH-02 override — same surgical inline edit approach.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite WEEKLY-06 acceptance text to match D-02</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (locate WEEKLY-06 bullet, currently at line 52)
    - .planning/phases/16-weekly-kpi-selection-scorecard-counters/16-CONTEXT.md §decisions D-01, D-02
  </read_first>
  <action>
Locate the WEEKLY-06 bullet in REQUIREMENTS.md (under `### Weekly KPI Selection Flow`, currently line 52). Replace the entire acceptance text verbatim:

FROM:
`- [ ] **WEEKLY-06**: Partner can change the weekly choice until the scorecard for that week is submitted; after submission, selection is locked`

TO:
`- [ ] **WEEKLY-06**: Partner commits their weekly KPI via a confirmation step; after commit, the partner cannot change the selection for that week — only Trace (admin) can override mid-week. Rationale: D-02 user override of the original pre-commit-change semantic (Phase 15 D-20/D-21 precedent — see Phase 16 CONTEXT).`

Preserve the checkbox state `[ ]` (unchecked). Do NOT touch any other bullet. Do NOT reflow the file. Do NOT modify traceability table at bottom (WEEKLY-06 row already says Phase 16 Pending — leave as-is).
  </action>
  <verify>
    <automated>grep -c "WEEKLY-06.*Partner commits their weekly KPI via a confirmation step" .planning/REQUIREMENTS.md && ! grep -q "WEEKLY-06.*until the scorecard for that week is submitted" .planning/REQUIREMENTS.md && grep -c "only Trace (admin) can override mid-week" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
New WEEKLY-06 text present (grep returns 1).
Old substring "until the scorecard for that week is submitted" absent (grep -q returns non-zero exit → command succeeds with the `!`).
"only Trace (admin) can override mid-week" appears exactly once.
No other requirement bullet modified (verified by git diff showing only the single line change).
  </done>
</task>

</tasks>

<verification>
- WEEKLY-06 text matches D-02 semantics.
- Pre-override substring removed from file.
- No collateral edits elsewhere in REQUIREMENTS.md.
- Git diff shows exactly one bullet rewritten.
</verification>

<success_criteria>
Any future agent reading REQUIREMENTS.md WEEKLY-06 implements the commit-time lock (D-02) — never the pre-override pre-submit-change semantic.
</success_criteria>

<output>
After completion, create `.planning/phases/16-weekly-kpi-selection-scorecard-counters/16-02-SUMMARY.md` following the standard summary template.
</output>
