---
phase: 16-weekly-kpi-selection-scorecard-counters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/content.js
  - src/index.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "WEEKLY_KPI_COPY export exists in src/data/content.js with selection/confirmation/success/error strings matching UI-SPEC verbatim"
    - "SCORECARD_COPY extended with growthPrefix, countLabel, reflectionLabel, reflectionPlaceholder, weeklyReflectionHeading, stickyNote, submitCta, emptyGuard*, submittedNotice keys"
    - "All new Phase 16 CSS classes exist in src/index.css with UI-SPEC verbatim values"
    - "No literal ' 7 ' or ' 5 ' KPI-count copy remains in SCORECARD_COPY / HUB_COPY (dynamic count)"
  artifacts:
    - path: "src/data/content.js"
      provides: "WEEKLY_KPI_COPY block + extended SCORECARD_COPY keys"
      contains: "export const WEEKLY_KPI_COPY"
    - path: "src/index.css"
      provides: "Phase 16 new CSS classes"
      contains: ".weekly-selection-subtext"
  key_links:
    - from: "src/data/content.js"
      to: "Phase 16 consumers (WeeklyKpiSelectionFlow, Scorecard, ThisWeekKpisSection)"
      via: "named import of WEEKLY_KPI_COPY and SCORECARD_COPY"
      pattern: "import \\{[^}]*WEEKLY_KPI_COPY"
---

<objective>
Add the copy contract (WEEKLY_KPI_COPY + SCORECARD_COPY extensions) and all net-new CSS classes required by every downstream Phase 16 task. This is Wave 1 foundation — pure config, zero runtime risk, unblocks Waves 2 and 3 to consume `content.js` imports and class names without inventing strings.

Purpose: Lock verbatim UI-SPEC strings and vanilla-CSS class definitions in one place so downstream plans reference them (no string duplication, no CSS drift).
Output: content.js augmented with copy; index.css augmented with ~15 new rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-weekly-kpi-selection-scorecard-counters/16-UI-SPEC.md
@.planning/phases/16-weekly-kpi-selection-scorecard-counters/16-PATTERNS.md
@.planning/phases/16-weekly-kpi-selection-scorecard-counters/16-CONTEXT.md
@src/data/content.js
@src/index.css

<interfaces>
<!-- Existing structure in content.js: KPI_COPY block at ~line 385, SCORECARD_COPY at ~line 450+. Same nested-object shape. -->
<!-- Existing CSS classes that MUST be reused (no re-style): .kpi-card, .kpi-card.selected, .kpi-card.capped, .kpi-category-tag, .kpi-counter, .scorecard-kpi-row, .scorecard-yn-btn, .scorecard-commit-gate, .btn-primary, .btn-ghost, .nav-row, .eyebrow, .scorecard-divider, .scorecard-reflection-label. -->

Phase 15 memory writes to preserve:
- "Trace" never "admin" in user-facing copy
- All copy lives in src/data/content.js (never hardcoded in components)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WEEKLY_KPI_COPY export + extend SCORECARD_COPY in content.js</name>
  <files>src/data/content.js</files>
  <read_first>
    - src/data/content.js (read entire file to locate KPI_COPY block ~line 385 and SCORECARD_COPY ~line 450; confirm nested-object convention)
    - .planning/phases/16-weekly-kpi-selection-scorecard-counters/16-UI-SPEC.md §Copywriting Contract (verbatim strings)
    - .planning/phases/16-weekly-kpi-selection-scorecard-counters/16-PATTERNS.md §src/data/content.js (exact code block to append)
  </read_first>
  <action>
Append a new export block AFTER the existing `KPI_COPY` block and BEFORE `SCORECARD_COPY`. Use single-quoted string literals and 2-space indent. Verbatim:

```js
export const WEEKLY_KPI_COPY = {
  selection: {
    eyebrow: 'Weekly KPI',
    heading: 'Choose Your KPI This Week',
    subtext: "You can change your mind until you confirm. After confirming, only Trace can update your selection.",
    disabledLabel: 'Used last week',
    emptyPool: 'No optional KPIs available — contact Trace.',
  },
  confirmation: {
    headingTemplate: (kpiLabel) => `Lock in ${kpiLabel} for this week?`,
    body: 'After confirming, only Trace can change your selection before next week.',
    confirmCta: 'Confirm Selection',
    backCta: 'Go back',
  },
  success: {
    heading: "You're locked in.",
    subtextTemplate: (kpiLabel) => `${kpiLabel} is your choice for this week.`,
    cta: 'Back to Hub',
  },
  errorBackToBack: 'This KPI was used last week. Choose a different one.',
  errorGeneric: "Couldn't save. Try again.",
  hubLockedLabel: 'Locked',
  hubLockedHeadingTemplate: (kpiLabel) => `This week: ${kpiLabel}`,
};
```

Then extend the existing `SCORECARD_COPY` object literal by ADDING these keys (do not rewrite existing keys):

```js
growthPrefix: 'GROWTH:',
countLabel: 'Count',
reflectionLabel: 'Reflection',
reflectionPlaceholder: 'What happened this week? What did you notice?',
weeklyReflectionHeading: 'Weekly Reflection',
tasksCompletedLabel: 'Tasks completed this week',
tasksCompletedPlaceholder: 'What did you finish?',
tasksCarriedOverLabel: 'Tasks carried over to next week',
tasksCarriedOverPlaceholder: 'What moves forward?',
biggestWinLabel: 'Biggest win',
biggestWinPlaceholder: 'What are you proud of?',
learningLabel: 'Learning this week',
learningPlaceholder: 'What did you discover or improve?',
weekRatingLabel: 'Week rating',
stickyNote: "This can't be undone.",
submitCta: 'Submit Scorecard',
submitErrorIncomplete: 'Rate each KPI as Met or Not Met before submitting.',
submitErrorDb: "Couldn't save your scorecard. Try again.",
submittedNotice: 'Submitted — nice work.',
emptyGuardHeading: 'No weekly KPI selected yet.',
emptyGuardBody: 'Head back to the hub and choose your KPI for this week first.',
emptyGuardCta: 'Go to Hub',
```

If any of the listed keys already exist in SCORECARD_COPY, leave the existing value — log which keys were skipped in the plan summary. Do NOT reorder or remove existing keys.

**Copy audit (Pitfall 5):** Grep the full file for literal ` 7 ` (with spaces) and ` 5 of ` patterns inside SCORECARD_COPY or HUB_COPY strings referencing KPI counts. If any literal KPI-count copy is found, replace with a function-template that takes count as a parameter (e.g. `(n) => \`${n} of ${n}...\``). If none found, no-op — log "No hardcoded KPI counts found in copy" in summary.
  </action>
  <verify>
    <automated>grep -n "export const WEEKLY_KPI_COPY" src/data/content.js && grep -n "hubLockedHeadingTemplate" src/data/content.js && grep -n "stickyNote:" src/data/content.js && grep -n "emptyGuardCta:" src/data/content.js && grep -n "submitCta: 'Submit Scorecard'" src/data/content.js && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
`grep -c "export const WEEKLY_KPI_COPY" src/data/content.js` returns 1.
`grep -c "hubLockedHeadingTemplate" src/data/content.js` returns ≥ 1.
All 21 SCORECARD_COPY extension keys (listed above) appear at least once in src/data/content.js.
`npm run build` succeeds without parse errors.
No literal " 7 " or " 5 of " KPI-count copy remains (verified by grep).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 16 CSS classes to src/index.css</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (locate existing .kpi-counter around line 811, .scorecard-kpi-row around line 947, .scorecard-commit-gate around line 930 — append new Phase 16 rules at END OF FILE)
    - .planning/phases/16-weekly-kpi-selection-scorecard-counters/16-UI-SPEC.md §Component Inventory (verbatim CSS values, lines 167-286)
  </read_first>
  <action>
APPEND (do not modify existing rules) the following CSS block at the END of src/index.css. Use 2-space indent. Values are UI-SPEC verbatim:

```css
/* ===== Phase 16: Weekly KPI Selection + Scorecard + Counters ===== */

/* WeeklyKpiSelectionFlow */
.weekly-selection-subtext {
  font-size: 15px;
  color: var(--muted);
  line-height: 1.55;
  margin-bottom: 24px;
}

.weekly-kpi-disabled-label {
  font-size: 12px;
  color: var(--muted-2);
  font-style: italic;
  margin-top: 4px;
  display: block;
}

.weekly-selection-error {
  font-size: 12px;
  color: var(--miss);
  margin-top: 8px;
}

/* Hub weekly-choice locked label */
.weekly-choice-locked-label {
  font-size: 12px;
  color: var(--muted-2);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-top: 4px;
  display: block;
}

/* Counter pill — +1 button + has-count modifier */
.kpi-counter-btn {
  background: none;
  border: none;
  cursor: pointer;
  font-size: 12px;
  font-weight: 700;
  color: var(--muted);
  padding: 0 4px;
  min-width: 32px;
  text-align: center;
}

.kpi-counter-btn:hover {
  color: var(--text);
}

.kpi-counter.has-count .kpi-counter-number {
  color: var(--gold);
}

/* Scorecard v2.0 row internals */
.scorecard-baseline-label {
  font-size: 15px;
  font-weight: 700;
  color: var(--text);
  line-height: 1.5;
  margin-bottom: 4px;
}

.scorecard-growth-clause {
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--muted);
  margin-bottom: 12px;
  line-height: 1.4;
}

.scorecard-count-field {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.scorecard-count-input {
  width: 80px;
}

.scorecard-reflection-block {
  display: flex;
  flex-direction: column;
  gap: 24px;
  margin-top: 32px;
}

.scorecard-reflection-field {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.scorecard-rating-row {
  display: flex;
  gap: 8px;
}

/* Sticky submit bar */
.scorecard-sticky-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 20;
  background: rgba(10, 10, 10, 0.88);
  backdrop-filter: blur(8px);
  border-top: 1px solid var(--border);
  padding: 12px 24px;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 16px;
}
```

Do NOT modify any existing CSS rule (no re-styling .kpi-card, .kpi-counter base, .scorecard-kpi-row, etc.). All changes are pure additions at the end of the file.
  </action>
  <verify>
    <automated>grep -c "^\.weekly-selection-subtext" src/index.css && grep -c "^\.kpi-counter-btn " src/index.css && grep -c "^\.kpi-counter\.has-count " src/index.css && grep -c "^\.scorecard-baseline-label" src/index.css && grep -c "^\.scorecard-sticky-bar" src/index.css && grep -c "^\.scorecard-growth-clause" src/index.css && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
All 14 new class selectors present (grep counts each ≥ 1): .weekly-selection-subtext, .weekly-kpi-disabled-label, .weekly-selection-error, .weekly-choice-locked-label, .kpi-counter-btn, .kpi-counter-btn:hover, .kpi-counter.has-count .kpi-counter-number, .scorecard-baseline-label, .scorecard-growth-clause, .scorecard-count-field, .scorecard-count-input, .scorecard-reflection-block, .scorecard-reflection-field, .scorecard-rating-row, .scorecard-sticky-bar.
`npm run build` succeeds (no CSS parse errors).
No existing class rules modified (verified by git diff — only additions at end of file).
  </done>
</task>

</tasks>

<verification>
- All 15 new CSS classes present in src/index.css (grep).
- WEEKLY_KPI_COPY exported from content.js.
- SCORECARD_COPY contains at minimum: growthPrefix, countLabel, reflectionLabel, reflectionPlaceholder, weeklyReflectionHeading, stickyNote, submitCta, emptyGuardCta, submittedNotice.
- `npm run build` succeeds.
- No existing CSS rule mutated (git diff shows additions only).
- No hardcoded " 7 " / " 5 of " KPI-count copy remains.
</verification>

<success_criteria>
Downstream plans (16-03, 16-04, 16-05) can import WEEKLY_KPI_COPY + extended SCORECARD_COPY from content.js and reference all new Phase 16 CSS classes without inventing strings or styles.
</success_criteria>

<output>
After completion, create `.planning/phases/16-weekly-kpi-selection-scorecard-counters/16-01-SUMMARY.md` following the standard summary template.
</output>
