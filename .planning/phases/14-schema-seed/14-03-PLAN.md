---
phase: 14-schema-seed
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SCHEMA-08

must_haves:
  truths:
    - "REQUIREMENTS.md SCHEMA-08 text reads '4 Theo role-mandatory + 4 Theo optional' (matching the PDF and D-02) — the stale '5 Theo optional' wording is gone"
    - "No other requirement text is modified (surgical edit only)"
    - "Requirement ID SCHEMA-08 still appears in the traceability table mapped to Phase 14"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected SCHEMA-08 text aligned with canonical PDF and Phase 14 CONTEXT D-02"
      contains: "4 Theo role-mandatory + 4 Theo optional"
  key_links: []
---

<objective>
Apply the one-line correction to REQUIREMENTS.md SCHEMA-08 text locked by Phase 14 CONTEXT D-02. The PDF is canonical; REQUIREMENTS.md's "5 Theo optional" wording predated PDF reconciliation and is wrong. This plan is a surgical doc fix that touches only `.planning/REQUIREMENTS.md` — a file not touched by Plan 14-01 or 14-02 — so it runs in Wave 1 independently (zero dependency on either).

Purpose: Prevents downstream confusion — future planners reading REQUIREMENTS.md will see the correct count and not introduce a 5-optional assumption into later phases.

Output: `.planning/REQUIREMENTS.md` with SCHEMA-08 bullet text updated. Nothing else changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/14-schema-seed/14-CONTEXT.md

<interfaces>
<!-- Current SCHEMA-08 text to be replaced (verbatim, from .planning/REQUIREMENTS.md line 20). -->

Current line (line ~20 of REQUIREMENTS.md):
```markdown
- [ ] **SCHEMA-08**: v2.0 reseed inserts 2 shared mandatory KPIs, 4 Theo role-mandatory + 5 Theo optional, 4 Jerry role-mandatory + 3 Jerry optional, 1 Jerry conditional sales KPI (inactive by default), plus mandatory personal growth priorities per partner and 7 business growth priority options
```

Target replacement (per D-02, verified against Cardinal_Role_KPI_Summary.pdf page 3):
```markdown
- [ ] **SCHEMA-08**: v2.0 reseed inserts 2 shared mandatory KPIs, 4 Theo role-mandatory + 4 Theo optional, 4 Jerry role-mandatory + 3 Jerry optional, 1 Jerry conditional sales KPI (inactive by default), plus mandatory personal growth priorities per partner and 7 business growth priority options
```

Only change: `5 Theo optional` → `4 Theo optional`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Correct SCHEMA-08 text in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (find current SCHEMA-08 line; verify no other SCHEMA-08 occurrences differ in text)
    - .planning/phases/14-schema-seed/14-CONTEXT.md (D-02 locks the correction)
  </read_first>
  <action>
Make a single surgical edit to `.planning/REQUIREMENTS.md`:

**Find this line (exact match, line ~20):**
```
- [ ] **SCHEMA-08**: v2.0 reseed inserts 2 shared mandatory KPIs, 4 Theo role-mandatory + 5 Theo optional, 4 Jerry role-mandatory + 3 Jerry optional, 1 Jerry conditional sales KPI (inactive by default), plus mandatory personal growth priorities per partner and 7 business growth priority options
```

**Replace with:**
```
- [ ] **SCHEMA-08**: v2.0 reseed inserts 2 shared mandatory KPIs, 4 Theo role-mandatory + 4 Theo optional, 4 Jerry role-mandatory + 3 Jerry optional, 1 Jerry conditional sales KPI (inactive by default), plus mandatory personal growth priorities per partner and 7 business growth priority options
```

Diff is exactly 1 character: `5` → `4` in the phrase "Theo optional".

Do NOT:
- Modify any other line in REQUIREMENTS.md
- Change the checkbox state (stays `- [ ]` — will flip to `[x]` only when Phase 14 executes, done at SUMMARY-writing time by the executor of Plan 14-01)
- Change the traceability table at the bottom (SCHEMA-08 → Phase 14 → Pending stays as-is until Phase 14 completion)
- Touch the status timestamp at the bottom of the file

After the edit, run a sanity grep to confirm "5 Theo optional" is gone from the file and "4 Theo optional" now appears.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8');if(f.includes('5 Theo optional')){console.error('STALE TEXT: \"5 Theo optional\" still present');process.exit(1)}if(!f.includes('4 Theo role-mandatory + 4 Theo optional')){console.error('MISSING: corrected text \"4 Theo role-mandatory + 4 Theo optional\" not found');process.exit(1)}if(!f.includes('| SCHEMA-08 | Phase 14 | Pending |')){console.error('TRACEABILITY: SCHEMA-08 row missing from traceability table (must stay intact)');process.exit(1)}console.log('SCHEMA-08 correction: PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` no longer contains the substring `5 Theo optional` anywhere
    - `.planning/REQUIREMENTS.md` contains the substring `4 Theo role-mandatory + 4 Theo optional` exactly once
    - Traceability table row `| SCHEMA-08 | Phase 14 | Pending |` is unchanged
    - No other bullet lines in the `### Schema & Seed (migration 009)` section were modified (executor can diff with git and confirm a 1-char change in one line)
    - File still ends with the trailing `*Last updated: ...*` line (no accidental truncation)
  </acceptance_criteria>
  <done>
REQUIREMENTS.md SCHEMA-08 text now matches canonical PDF and Phase 14 CONTEXT D-02. No other content touched.
  </done>
</task>

</tasks>

<verification>
Phase 14 Plan 03 passes when:
- `git diff .planning/REQUIREMENTS.md` shows exactly 1 changed line, exactly 1 character differing (`5` → `4`)
- grep for "5 Theo optional" returns no results
- grep for "4 Theo role-mandatory + 4 Theo optional" returns exactly 1 result
</verification>

<success_criteria>
Maps to ROADMAP Phase 14 success — supports (not gates) the 5 must_haves by removing a documentation trap that would otherwise mislead future phase planners.
</success_criteria>

<output>
After completion, create `.planning/phases/14-schema-seed/14-03-SUMMARY.md` with:
- Single-line diff summary (before/after)
- Rationale citation: D-02 + Cardinal_Role_KPI_Summary.pdf page 3 "Choose 1 Additional KPI" section listing exactly 4 Theo options
</output>
</content>
</invoke>