---
phase: 04-admin-tools-meeting-mode
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/admin/AdminScorecards.jsx
  - src/components/KpiSelectionView.jsx
  - src/components/admin/AdminPartners.jsx
autonomous: true
requirements:
  - ADMIN-05
  - ADMIN-06
must_haves:
  truths:
    - "Admin can cycle growth priority status (active -> achieved -> stalled -> deferred -> active) from AdminPartners with immediate persistence"
    - "Admin can type and blur-save an admin_note on each growth priority row"
    - "Partners see growth status badge + admin note on /kpi-view/:partner (KpiSelectionView)"
    - "Admin can open /admin/scorecards page and see both partners' weekly history"
    - "Admin can reopen a closed week via two-click arm/confirm — stamps admin_reopened_at"
    - "AdminScorecards uses local isAdminClosed() helper that accounts for admin_reopened_at — src/lib/week.js isWeekClosed is NOT modified (Pitfall 5)"
    - "Scorecard history rendering uses getLabelForEntry() render-time fallback for D-06 label snapshot"
  artifacts:
    - path: "src/components/admin/AdminScorecards.jsx"
      provides: "/admin/scorecards cross-partner weekly history + reopen closed week"
      min_lines: 200
    - path: "src/components/KpiSelectionView.jsx"
      provides: "Partner-facing read-only view renders growth status badge + admin note"
    - path: "src/components/admin/AdminPartners.jsx"
      provides: "PartnerSection has growth status cycle + admin note editor + View Scorecard History deep link"
  key_links:
    - from: "src/components/admin/AdminPartners.jsx"
      to: "src/lib/supabase.js updateGrowthPriorityStatus + updateGrowthPriorityAdminNote"
      via: "onClick status cycle; onBlur note save"
      pattern: "updateGrowthPriorityStatus|updateGrowthPriorityAdminNote"
    - from: "src/components/KpiSelectionView.jsx"
      to: "src/data/content.js GROWTH_STATUS_COPY"
      via: "import + render status badge"
      pattern: "GROWTH_STATUS_COPY"
    - from: "src/components/admin/AdminScorecards.jsx"
      to: "src/lib/supabase.js reopenScorecardWeek + fetchScorecards + fetchKpiSelections"
      via: "import + two-click arm/confirm reopen"
      pattern: "reopenScorecardWeek"
    - from: "src/components/admin/AdminScorecards.jsx"
      to: "src/lib/week.js isWeekClosed"
      via: "import and wrap with local isAdminClosed() (Pattern 7 / Pitfall 5)"
      pattern: "isAdminClosed"
---

<objective>
Build the Growth Priority admin editing surface (in AdminPartners) and the Scorecard Oversight page (/admin/scorecards). Also surface growth status + admin note to partners on KpiSelectionView.

Purpose: Closes ADMIN-05, ADMIN-06 and clears Phase 3 D-17 (admin reopen closed week). This plan touches three files and splits into three tasks.
Output: New AdminScorecards.jsx component; KpiSelectionView.jsx extended with growth display; AdminPartners.jsx extended with growth editor + scorecards deep link. Route registration for /admin/scorecards is deferred to P04-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-tools-meeting-mode/04-CONTEXT.md
@.planning/phases/04-admin-tools-meeting-mode/04-RESEARCH.md
@.planning/phases/04-admin-tools-meeting-mode/04-UI-SPEC.md
@.planning/phases/04-admin-tools-meeting-mode/04-01-SUMMARY.md
@CLAUDE.md

@src/components/admin/AdminPartners.jsx
@src/components/KpiSelectionView.jsx
@src/components/Scorecard.jsx
@src/lib/week.js
@src/lib/supabase.js
@src/data/content.js

<interfaces>
<!-- After P04-01, available from src/lib/supabase.js -->

```js
// Growth priority admin
export async function updateGrowthPriorityStatus(id, status); // 'active'|'achieved'|'stalled'|'deferred'
export async function updateGrowthPriorityAdminNote(id, adminNote);

// Scorecard admin
export async function reopenScorecardWeek(partner, weekOf); // stamps admin_reopened_at

// Existing helpers reused
export async function fetchGrowthPriorities(partner); // rows have status + admin_note after migration 005
export async function fetchScorecards(partner);
export async function fetchKpiSelections(partner);
```

From src/lib/week.js (existing, DO NOT MODIFY per Pitfall 5):
```js
export function getMondayOf(d);
export function getSundayEndOf(mondayStr);
export function isWeekClosed(mondayStr); // Pure date check — DO NOT modify
export function formatWeekRange(mondayStr);
```

From src/data/content.js (added by P04-01):
```js
GROWTH_STATUS_COPY = { active, achieved, stalled, deferred, adminNoteLabel, adminNotePlaceholder(name), savedFlash };
ADMIN_SCORECARD_COPY = { eyebrow, heading, reopenBtn, reopenConfirmBtn, reopenWarning(name), reopenedBadge, overrideMarker, empty, errors };
ADMIN_GROWTH_COPY = { eyebrow, savedFlash, errors };
```

CSS classes available from P04-01 (src/index.css):
- `.growth-status-badge`, `.growth-status-badge.active|achieved|stalled|deferred`
- `.growth-admin-note`
- `.scorecard-oversight-grid`, `.scorecard-oversight-row`, `.scorecard-oversight-header`, `.scorecard-oversight-cell`, `.scorecard-oversight-cell.week`
- `.scorecard-reopened-badge`
- `.meeting-admin-override-marker`

Status cycle order (D-09 + UI-SPEC): `active -> achieved -> stalled -> deferred -> active`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend AdminPartners PartnerSection with growth editor + scorecards deep link</name>
  <files>src/components/admin/AdminPartners.jsx</files>
  <read_first>
    - src/components/admin/AdminPartners.jsx (full — find loadState which already fetches `growth` via Promise.all; find PartnerSection render structure; find nav row where P04-02 added "Manage KPIs" link)
    - src/lib/supabase.js (verify updateGrowthPriorityStatus + updateGrowthPriorityAdminNote exist from P04-01)
    - src/data/content.js (verify GROWTH_STATUS_COPY + ADMIN_GROWTH_COPY exist from P04-01)
    - .planning/phases/04-admin-tools-meeting-mode/04-UI-SPEC.md (lines 207-211 — Growth Priority Status Toggle interaction; lines 137-145 — badge CSS classes)
  </read_first>
  <action>
Modify `src/components/admin/AdminPartners.jsx`:

STEP 1 — Imports. Add to existing supabase import block:
  updateGrowthPriorityStatus, updateGrowthPriorityAdminNote

Add named imports from content.js (PARTNER_DISPLAY is already imported):
  GROWTH_STATUS_COPY, ADMIN_GROWTH_COPY

STEP 2 — Add module-level constant + helper near top (outside component):
  const STATUS_CYCLE = ['active', 'achieved', 'stalled', 'deferred'];
  function nextStatus(current) {
    const i = STATUS_CYCLE.indexOf(current);
    return STATUS_CYCLE[(i + 1) % STATUS_CYCLE.length];
  }

STEP 3 — Inside `PartnerSection`, add new state:
  const [growthSaving, setGrowthSaving] = useState({}); // { [id]: 'status'|'note'|null }
  const [growthError, setGrowthError] = useState('');
  const [noteDrafts, setNoteDrafts] = useState({}); // { [id]: string }

STEP 4 — Add handlers inside `PartnerSection`:
  async function handleCycleStatus(priorityId, currentStatus) {
    setGrowthSaving((s) => ({ ...s, [priorityId]: 'status' }));
    setGrowthError('');
    try {
      const ns = nextStatus(currentStatus || 'active');
      await updateGrowthPriorityStatus(priorityId, ns);
      await loadState();
    } catch (err) {
      console.error(err);
      setGrowthError(ADMIN_GROWTH_COPY.errors.statusFail);
    } finally {
      setGrowthSaving((s) => ({ ...s, [priorityId]: null }));
    }
  }

  async function handleSaveNote(priorityId) {
    const text = noteDrafts[priorityId] ?? '';
    setGrowthSaving((s) => ({ ...s, [priorityId]: 'note' }));
    setGrowthError('');
    try {
      await updateGrowthPriorityAdminNote(priorityId, text);
      await loadState();
    } catch (err) {
      console.error(err);
      setGrowthError(ADMIN_GROWTH_COPY.errors.noteFail);
    } finally {
      setGrowthSaving((s) => ({ ...s, [priorityId]: null }));
    }
  }

STEP 5 — Seed note drafts when `growth` changes. Add a useEffect:
  useEffect(() => {
    const seeded = {};
    for (const g of growth) seeded[g.id] = g.admin_note ?? '';
    setNoteDrafts(seeded);
  }, [growth]);

STEP 6 — Render a new "Growth Priorities" subsection inside PartnerSection JSX (between the existing KPI display block and the reset buttons, or in a similar natural location). For each `growth` row render a card-style block:

  <div className="admin-growth-row" style={{marginTop:16, padding:16, background:'var(--surface)', border:'1px solid var(--border)', borderRadius:12}}>
    <div>
      {g.description}
      <span className="muted" style={{marginLeft:8, fontSize:12}}>({g.type})</span>
    </div>
    <button
      type="button"
      className={`growth-status-badge ${g.status || 'active'}`}
      onClick={() => handleCycleStatus(g.id, g.status)}
      disabled={growthSaving[g.id] === 'status'}
      style={{marginTop:8, cursor:'pointer'}}
    >
      {GROWTH_STATUS_COPY[g.status || 'active']}
    </button>
    <div style={{marginTop:12}}>
      <div className="eyebrow">{GROWTH_STATUS_COPY.adminNoteLabel}</div>
      <textarea
        value={noteDrafts[g.id] ?? ''}
        onChange={(e) => setNoteDrafts((d) => ({ ...d, [g.id]: e.target.value }))}
        onBlur={() => handleSaveNote(g.id)}
        placeholder={GROWTH_STATUS_COPY.adminNotePlaceholder(PARTNER_DISPLAY[partner] || partner)}
        rows={3}
        className="input"
        style={{width:'100%', resize:'vertical'}}
      />
    </div>
  </div>

Wrap the loop with a heading: `<div className="eyebrow" style={{marginTop:24}}>{ADMIN_GROWTH_COPY.eyebrow}</div>`

If `growthError` is non-empty, show `<div className="muted" style={{color:'var(--red)'}}>{growthError}</div>` below the section.

STEP 7 — Add "View Scorecard History" Link button in the same nav row where the "Manage KPIs" Link was added by P04-02:
  <Link
    to={`/admin/scorecards?partner=${partner}`}
    className="btn btn-ghost"
    style={{ textDecoration: 'none' }}
  >
    View Scorecard History
  </Link>

DO NOT remove any existing functionality (ResetButton, performReset, loadState, existing Link buttons, existing KPI/scorecard display). DO NOT modify the existing growth fetch in loadState — it already returns rows.
  </action>
  <verify>
    <automated>grep -q "updateGrowthPriorityStatus" src/components/admin/AdminPartners.jsx && grep -q "updateGrowthPriorityAdminNote" src/components/admin/AdminPartners.jsx && grep -q "GROWTH_STATUS_COPY" src/components/admin/AdminPartners.jsx && grep -q "growth-status-badge" src/components/admin/AdminPartners.jsx && grep -q "View Scorecard History" src/components/admin/AdminPartners.jsx && grep -q "admin/scorecards?partner=" src/components/admin/AdminPartners.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "updateGrowthPriorityStatus" src/components/admin/AdminPartners.jsx
    - grep -q "updateGrowthPriorityAdminNote" src/components/admin/AdminPartners.jsx
    - grep -q "GROWTH_STATUS_COPY" src/components/admin/AdminPartners.jsx
    - grep -q "ADMIN_GROWTH_COPY" src/components/admin/AdminPartners.jsx
    - grep -q "STATUS_CYCLE" src/components/admin/AdminPartners.jsx
    - grep -q "nextStatus" src/components/admin/AdminPartners.jsx
    - grep -q "handleCycleStatus" src/components/admin/AdminPartners.jsx
    - grep -q "handleSaveNote" src/components/admin/AdminPartners.jsx
    - grep -q "growth-status-badge" src/components/admin/AdminPartners.jsx
    - grep -q "View Scorecard History" src/components/admin/AdminPartners.jsx
    - grep -q "admin/scorecards?partner=" src/components/admin/AdminPartners.jsx
    - grep -q "admin/kpi?partner=" src/components/admin/AdminPartners.jsx (P04-02 link preserved)
    - grep -q "ResetButton" src/components/admin/AdminPartners.jsx (existing component preserved)
    - grep -q "performReset" src/components/admin/AdminPartners.jsx (existing handler preserved)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>AdminPartners PartnerSection has a working growth priority editor (status cycle + admin note textarea) and a View Scorecard History deep link. Existing functionality preserved. Build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend KpiSelectionView with growth status badge + admin note (partner-facing)</name>
  <files>src/components/KpiSelectionView.jsx</files>
  <read_first>
    - src/components/KpiSelectionView.jsx (full — find where growth priorities are rendered; understand current layout)
    - src/data/content.js (verify GROWTH_STATUS_COPY exists from P04-01)
    - .planning/phases/04-admin-tools-meeting-mode/04-CONTEXT.md (D-11 — partner-facing status badge + admin_note rendering)
    - .planning/phases/04-admin-tools-meeting-mode/04-UI-SPEC.md (lines 181-184 — KpiSelectionView badge reuse)
  </read_first>
  <action>
Modify `src/components/KpiSelectionView.jsx` to render the growth priority status badge and admin note alongside each growth priority item.

STEP 1 — Add import:
  import { GROWTH_STATUS_COPY } from '../data/content.js';
  (Note: if content.js is already imported, add GROWTH_STATUS_COPY to the existing destructured import.)

STEP 2 — Locate the JSX block that renders each growth priority row (it will be a .map or similar over growth_priorities fetched in the component). Inside that render block, for each growth row `g`, add two new elements below the existing description/text:

  <span className={`growth-status-badge ${g.status || 'active'}`}>
    {GROWTH_STATUS_COPY[g.status || 'active']}
  </span>
  {g.admin_note && g.admin_note.trim() ? (
    <div className="growth-admin-note">
      <div className="eyebrow" style={{marginBottom:4}}>{GROWTH_STATUS_COPY.adminNoteLabel}</div>
      {g.admin_note}
    </div>
  ) : null}

STEP 3 — Verify the fetched growth rows include `status` and `admin_note` fields. Since fetchGrowthPriorities already does `select('*')`, both columns will be present after migration 005 applies. No changes to the fetch.

DO NOT modify the KPI slot rendering or any navigation logic. Only extend the growth priority display.
  </action>
  <verify>
    <automated>grep -q "GROWTH_STATUS_COPY" src/components/KpiSelectionView.jsx && grep -q "growth-status-badge" src/components/KpiSelectionView.jsx && grep -q "growth-admin-note" src/components/KpiSelectionView.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "import.*GROWTH_STATUS_COPY" src/components/KpiSelectionView.jsx
    - grep -q "growth-status-badge" src/components/KpiSelectionView.jsx
    - grep -q "growth-admin-note" src/components/KpiSelectionView.jsx
    - grep -q "g.admin_note\|admin_note" src/components/KpiSelectionView.jsx
    - Existing rendering of growth priority descriptions is preserved (grep the phrase "growth" or inspect file — previous render blocks unchanged)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>KpiSelectionView renders a .growth-status-badge for each growth priority using the current status value, and renders the admin_note in a .growth-admin-note block when non-empty. Build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create AdminScorecards.jsx (/admin/scorecards) with reopen closed week</name>
  <files>src/components/admin/AdminScorecards.jsx</files>
  <read_first>
    - src/lib/supabase.js (verify fetchScorecards, fetchKpiSelections, reopenScorecardWeek exist)
    - src/lib/week.js (understand isWeekClosed, getSundayEndOf, formatWeekRange)
    - src/components/Scorecard.jsx (reference: how kpi_results is rendered; structure of a scorecard row)
    - src/components/admin/AdminPartners.jsx (ResetButton two-click pattern; loadState + Promise.all)
    - src/data/content.js (ADMIN_SCORECARD_COPY shape; PARTNER_DISPLAY)
    - .planning/phases/04-admin-tools-meeting-mode/04-RESEARCH.md (Pattern 6 — getLabelForEntry render-time fallback; Pattern 7 — isAdminClosed; Pitfall 5 — do not modify week.js)
    - .planning/phases/04-admin-tools-meeting-mode/04-UI-SPEC.md (lines 168-175 — Admin Scorecard Oversight CSS; lines 213-215 — Interaction Contract reopen)
    - .planning/phases/04-admin-tools-meeting-mode/04-CONTEXT.md (D-21 — reopen only on /admin/scorecards; D-06 — label snapshot fallback)
  </read_first>
  <action>
Create `src/components/admin/AdminScorecards.jsx` as a new default-exported React component for route `/admin/scorecards`.

STRUCTURE:
```jsx
import { useState, useEffect, useCallback, useRef } from 'react';
import { Link, useSearchParams } from 'react-router-dom';
import {
  fetchScorecards,
  fetchKpiSelections,
  reopenScorecardWeek,
} from '../../lib/supabase.js';
import { isWeekClosed, formatWeekRange } from '../../lib/week.js';
import { ADMIN_SCORECARD_COPY, PARTNER_DISPLAY } from '../../data/content.js';

const MANAGED = ['theo', 'jerry'];
```

DEFINE LOCAL HELPERS at module level (outside component):
```js
// Pattern 7: admin-aware closed check. Uses isWeekClosed for date derivation,
// but overrides when admin_reopened_at is set (D-21). DO NOT modify week.js isWeekClosed.
function isAdminClosed(row) {
  if (!row) return false;
  if (row.admin_reopened_at) return false; // admin explicitly reopened
  return isWeekClosed(row.week_of);
}

// Pattern 6: render-time label fallback for kpi_results (D-06).
// New rows have entry.label; Phase-3 rows do not.
function getLabelForEntry(kpiId, entry, lockedKpis) {
  if (entry && entry.label) return entry.label;
  const match = lockedKpis.find((k) => k.id === kpiId);
  return match?.label_snapshot ?? '(unknown KPI)';
}
```

DEFAULT EXPORT `AdminScorecards`:
- State: `loading` (bool), `error` (string), `data` ({ theo: { scorecards, kpis }, jerry: { scorecards, kpis } }), `pendingReopen` ({ [key: partner + week_of]: bool }), `reopeningKey` (string | null), `feedback` (string)
- Read searchParams for `?partner=...` to optionally focus/scroll (not required for functionality)
- loadState useCallback: `Promise.all([fetchScorecards('theo'), fetchKpiSelections('theo'), fetchScorecards('jerry'), fetchKpiSelections('jerry')])`, set data. `.catch(console.error)`, `setError(...)` on failure.
- useEffect on mount: call loadState
- Two-click arm/confirm for reopen: `pendingReopen` keyed on `${partner}_${weekOf}`. First click sets pending; second click in the same arm window (3s) calls `reopenScorecardWeek(partner, weekOf)`, then reloads. Use a useRef for setTimeout to auto-disarm.

RENDER:
- app-shell + brand header + "Back to Admin Hub" ghost button (model after AdminPartners / AdminKpi)
- screen-header with eyebrow from ADMIN_SCORECARD_COPY.eyebrow ("SCORECARD OVERSIGHT") and h2 from ADMIN_SCORECARD_COPY.heading ("Weekly Check-In History")
- For each partner in MANAGED order, render a section:
  - Section header: PARTNER_DISPLAY[p] + partner name
  - `.scorecard-oversight-grid` container
  - Map `data[p].scorecards` (array, already sorted newest-first by fetchScorecards). For each scorecard row:
    - `.scorecard-oversight-row`
    - Inside, show:
      - Week column: `.scorecard-oversight-cell.week` showing `formatWeekRange(row.week_of)`
      - If `row.admin_reopened_at` is set, show `.scorecard-reopened-badge` with ADMIN_SCORECARD_COPY.reopenedBadge ("Reopened") beside the week
      - If `row.admin_override_at` is set, show `.meeting-admin-override-marker` with ADMIN_SCORECARD_COPY.overrideMarker ("Edited by admin")
      - KPI results: iterate `row.kpi_results` entries. For each `[kpiId, entry]`, render a row with `getLabelForEntry(kpiId, entry, data[p].kpis)` + result indicator (yes/no dot or text) + entry.reflection (if present)
      - Status column: "Closed" or "Active" based on `isAdminClosed(row)` (if closed → show Reopen button; if not closed → show nothing or muted "Editable")
      - Reopen button: only visible when `isAdminClosed(row)` is true (and admin_reopened_at is null). Two-click arm/confirm:
        - Unarmed state: button shows ADMIN_SCORECARD_COPY.reopenBtn ("Reopen Week")
        - Armed state: button shows ADMIN_SCORECARD_COPY.reopenConfirmBtn ("Confirm Reopen"), background tint red, plus a warning div below showing `ADMIN_SCORECARD_COPY.reopenWarning(PARTNER_DISPLAY[p])`
  - Empty state (`data[p].scorecards.length === 0`): show ADMIN_SCORECARD_COPY.empty ("No completed check-ins yet.") as `.muted` text
- Error display (`error` state) shown as muted red notice near top

DO NOT:
- Modify src/lib/week.js
- Modify src/components/Scorecard.jsx (partner-facing)
- Add the route to App.jsx (deferred to P04-05)
- Add inline per-entry editing (admin override lives in Meeting Mode per D-15; this page is read + reopen only)

File estimated 200-280 lines.
  </action>
  <verify>
    <automated>test -f src/components/admin/AdminScorecards.jsx && grep -q "export default function AdminScorecards" src/components/admin/AdminScorecards.jsx && grep -q "reopenScorecardWeek" src/components/admin/AdminScorecards.jsx && grep -q "isAdminClosed" src/components/admin/AdminScorecards.jsx && grep -q "getLabelForEntry" src/components/admin/AdminScorecards.jsx && grep -q "ADMIN_SCORECARD_COPY" src/components/admin/AdminScorecards.jsx && grep -q "scorecard-oversight-grid" src/components/admin/AdminScorecards.jsx && grep -q "scorecard-reopened-badge" src/components/admin/AdminScorecards.jsx</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/admin/AdminScorecards.jsx exists
    - Contains `export default function AdminScorecards`
    - Imports: fetchScorecards, fetchKpiSelections, reopenScorecardWeek from '../../lib/supabase.js'
    - Imports: isWeekClosed, formatWeekRange from '../../lib/week.js'
    - Imports: ADMIN_SCORECARD_COPY, PARTNER_DISPLAY from '../../data/content.js'
    - Defines a local `isAdminClosed` function that checks `admin_reopened_at` before delegating to `isWeekClosed`
    - Defines a local `getLabelForEntry` function with entry.label fallback
    - Contains JSX className "scorecard-oversight-grid"
    - Contains JSX className "scorecard-oversight-row"
    - Contains JSX className "scorecard-reopened-badge"
    - Contains a two-click reopen pattern (pendingReopen state or similar)
    - src/lib/week.js is NOT modified (verified by checking git diff or by confirming the file's isWeekClosed signature is unchanged — grep for original function still returns single-line return)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>AdminScorecards.jsx created with cross-partner history, isAdminClosed local helper, getLabelForEntry fallback, two-click reopen, and does not modify week.js. Build passes.</done>
</task>

</tasks>

<verification>
After all three tasks:

1. Build passes:
```
npm run build
```

2. AdminPartners extended:
```
grep -c "updateGrowthPriorityStatus\|updateGrowthPriorityAdminNote\|View Scorecard History" src/components/admin/AdminPartners.jsx
# Expected: at least 3
```

3. KpiSelectionView extended:
```
grep -q "growth-status-badge" src/components/KpiSelectionView.jsx
```

4. AdminScorecards created with all required helpers:
```
test -f src/components/admin/AdminScorecards.jsx
grep -q "isAdminClosed" src/components/admin/AdminScorecards.jsx
grep -q "getLabelForEntry" src/components/admin/AdminScorecards.jsx
```

5. week.js untouched (Pitfall 5 compliance):
```
git diff --stat src/lib/week.js
# Expected: no output (file not in diff)
```
</verification>

<success_criteria>
- AdminPartners PartnerSection has a working growth priority editor with status cycle + admin note textarea
- AdminPartners has View Scorecard History deep link pointing to /admin/scorecards?partner={p}
- KpiSelectionView renders growth status badge + admin_note for each growth priority (partner-facing)
- AdminScorecards.jsx exists with cross-partner history, isAdminClosed wrapper, getLabelForEntry fallback, two-click reopen
- src/lib/week.js is NOT modified (Pitfall 5)
- src/components/Scorecard.jsx (partner-facing) is NOT modified
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-tools-meeting-mode/04-03-SUMMARY.md` covering:
- AdminPartners growth editor implementation notes (state shape, handlers, side effects)
- KpiSelectionView growth display additions (location in render tree)
- AdminScorecards component structure (loadState shape, isAdminClosed + getLabelForEntry helpers)
- Confirmation that src/lib/week.js and src/components/Scorecard.jsx were NOT modified
- Any deviations
</output>
