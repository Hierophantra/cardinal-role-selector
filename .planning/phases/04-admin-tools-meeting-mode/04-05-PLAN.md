---
phase: 04-admin-tools-meeting-mode
plan: 05
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
  - "04-04"
files_modified:
  - src/App.jsx
  - src/components/admin/AdminHub.jsx
files_created: []
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-04
  - MEET-01
must_haves:
  truths:
    - "App.jsx registers four new routes: /admin/kpi, /admin/scorecards, /admin/meeting, /admin/meeting/:id"
    - "AdminHub shows Meeting Mode as a hero card ABOVE the section grid (full width, red left-border accent)"
    - "AdminHub Accountability section has KPI Management, Scorecard Oversight, and (old) Meeting Mode card REPLACED — the hero promotion means Meeting Mode appears ONLY in the hero position, not duplicated in the grid"
    - "KPI Management card and Scorecard Oversight card in the grid are NOT disabled — they link to /admin/kpi and /admin/scorecards respectively"
    - "ROADMAP.md Phase 4 success criteria checkboxes are marked complete"
  artifacts:
    - path: "src/App.jsx"
      provides: "Routes registered for all 4 new admin pages"
      contains: "/admin/kpi"
    - path: "src/components/admin/AdminHub.jsx"
      provides: "Hero Meeting Mode card + enabled KPI Management + enabled Scorecard Oversight cards"
      contains: "hub-card--hero"
  key_links:
    - from: "src/App.jsx"
      to: "src/components/admin/AdminKpi.jsx"
      via: "<Route path='/admin/kpi' element={<AdminKpi />} />"
      pattern: "AdminKpi"
    - from: "src/App.jsx"
      to: "src/components/admin/AdminScorecards.jsx"
      via: "<Route path='/admin/scorecards' element={<AdminScorecards />} />"
      pattern: "AdminScorecards"
    - from: "src/App.jsx"
      to: "src/components/admin/AdminMeeting.jsx"
      via: "<Route path='/admin/meeting' element={<AdminMeeting />} />"
      pattern: "AdminMeeting"
    - from: "src/App.jsx"
      to: "src/components/admin/AdminMeetingSession.jsx"
      via: "<Route path='/admin/meeting/:id' element={<AdminMeetingSession />} />"
      pattern: "AdminMeetingSession"
    - from: "src/components/admin/AdminHub.jsx"
      to: "/admin/meeting"
      via: "Link wrapping the hero card"
      pattern: "hub-card--hero"
---

<objective>
Wire all Phase 4 components into the application: register the four new routes in App.jsx, and update AdminHub to promote Meeting Mode to a hero card above the section grid while enabling the previously-disabled KPI Management and Scorecard Oversight cards. Also finalize ROADMAP.md checkboxes for Phase 4.

Purpose: The final wave — depends on plans 02, 03, and 04 all landing their component files. This plan is intentionally small (2 tasks, both minimally invasive) so the phase can ship cleanly with a verification checkpoint.
Output: Wired routes + updated hub = Phase 4 shippable state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-tools-meeting-mode/04-CONTEXT.md
@.planning/phases/04-admin-tools-meeting-mode/04-UI-SPEC.md
@.planning/phases/04-admin-tools-meeting-mode/04-02-SUMMARY.md
@.planning/phases/04-admin-tools-meeting-mode/04-03-SUMMARY.md
@.planning/phases/04-admin-tools-meeting-mode/04-04-SUMMARY.md
@CLAUDE.md

@src/App.jsx
@src/components/admin/AdminHub.jsx
@src/data/content.js

<interfaces>
<!-- After P04-02, P04-03, P04-04 land, these components exist: -->

- src/components/admin/AdminKpi.jsx — default export `AdminKpi` (route /admin/kpi)
- src/components/admin/AdminScorecards.jsx — default export `AdminScorecards` (route /admin/scorecards)
- src/components/admin/AdminMeeting.jsx — default export `AdminMeeting` (route /admin/meeting)
- src/components/admin/AdminMeetingSession.jsx — default export `AdminMeetingSession` (route /admin/meeting/:id)

From src/data/content.js (updated by P04-01):
- HUB_COPY.admin.cards.kpiManagement — { title, description } (no disabledLabel)
- HUB_COPY.admin.cards.meetingMode — { title, description } (no disabledLabel) — used for the hero card
- HUB_COPY.admin.cards.scorecardOversight — { title, description } (new, added by P04-01)

CSS class available from P04-01:
- `.hub-card--hero` — red left-border, linear gradient bg, full-width (grid-column 1/-1 or standalone)

Existing AdminHub.jsx structure:
- Imports: fetchSubmissions, fetchKpiSelections; HUB_COPY
- Renders: status summary, PARTNERS hub-section with dashboard/partnerProfiles/comparison/test cards, ACCOUNTABILITY hub-section with two DISABLED cards (kpiManagement + meetingMode)
- Lines 116-133 in current file are the accountability section to rework
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register four new routes in App.jsx</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (full — find existing Route declarations; understand import pattern)
    - src/components/admin/AdminKpi.jsx (verify default export name — created by P04-02)
    - src/components/admin/AdminScorecards.jsx (verify default export name — created by P04-03)
    - src/components/admin/AdminMeeting.jsx (verify default export name — created by P04-04)
    - src/components/admin/AdminMeetingSession.jsx (verify default export name — created by P04-04)
  </read_first>
  <action>
Modify `src/App.jsx` to add imports and route entries for the four new Phase 4 components.

STEP 1 — Add imports at the top of App.jsx (alongside existing admin component imports):

```jsx
import AdminKpi from './components/admin/AdminKpi.jsx';
import AdminScorecards from './components/admin/AdminScorecards.jsx';
import AdminMeeting from './components/admin/AdminMeeting.jsx';
import AdminMeetingSession from './components/admin/AdminMeetingSession.jsx';
```

STEP 2 — Add four new Route entries inside `<Routes>`, placed AFTER existing admin routes (e.g., after `/admin/test`) and BEFORE the catch-all `<Route path="*" element={<Navigate to="/" replace />} />`:

```jsx
<Route path="/admin/kpi" element={<AdminKpi />} />
<Route path="/admin/scorecards" element={<AdminScorecards />} />
<Route path="/admin/meeting" element={<AdminMeeting />} />
<Route path="/admin/meeting/:id" element={<AdminMeetingSession />} />
```

IMPORTANT — ROUTE ORDER:
- `/admin/meeting/:id` must come AFTER `/admin/meeting` so the plain route is matched first when there is no id segment. React Router v6 actually handles precedence automatically, but placing them in this order is still clearer.
- Keep the catch-all `/` route last.

DO NOT remove any existing routes. DO NOT modify the BrowserRouter setup (it's in main.jsx). DO NOT touch the Login, Questionnaire, or other non-admin routes.
  </action>
  <verify>
    <automated>grep -q "import AdminKpi from './components/admin/AdminKpi.jsx'" src/App.jsx && grep -q "import AdminScorecards from './components/admin/AdminScorecards.jsx'" src/App.jsx && grep -q "import AdminMeeting from './components/admin/AdminMeeting.jsx'" src/App.jsx && grep -q "import AdminMeetingSession from './components/admin/AdminMeetingSession.jsx'" src/App.jsx && grep -q 'path="/admin/kpi"' src/App.jsx && grep -q 'path="/admin/scorecards"' src/App.jsx && grep -q 'path="/admin/meeting"' src/App.jsx && grep -q 'path="/admin/meeting/:id"' src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep finds all four new import statements with correct relative paths
    - grep finds all four new Route path strings: "/admin/kpi", "/admin/scorecards", "/admin/meeting", "/admin/meeting/:id"
    - The catch-all Route with `path="*"` is still present and still placed after all specific routes
    - Existing routes (/admin, /admin/hub, /admin/partners, /admin/profile/:partner, /admin/comparison, /admin/test, /kpi/:partner, /kpi-view/:partner, /scorecard/:partner) are all preserved
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>App.jsx registers four new Phase 4 routes; build passes; all existing routes preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update AdminHub with hero Meeting Mode card + enabled KPI Management + Scorecard Oversight cards</name>
  <files>src/components/admin/AdminHub.jsx</files>
  <read_first>
    - src/components/admin/AdminHub.jsx (full — understand current render tree; find ACCOUNTABILITY section at lines 116-133; identify .hub-card--disabled usage)
    - src/data/content.js (verify HUB_COPY.admin.cards.kpiManagement + meetingMode no longer have disabledLabel from P04-01; verify scorecardOversight exists from P04-01)
    - .planning/phases/04-admin-tools-meeting-mode/04-UI-SPEC.md (lines 147-153 — .hub-card--hero class; lines 228-231 — hero card copy)
    - .planning/phases/04-admin-tools-meeting-mode/04-CONTEXT.md (D-02 — Meeting Mode promoted to hero card above section grid)
    - .planning/phases/04-admin-tools-meeting-mode/04-RESEARCH.md (Pitfall 6 — hero card must be OUTSIDE .hub-grid to render full width since it's a standalone element between screen-header and first hub-section)
  </read_first>
  <action>
Modify `src/components/admin/AdminHub.jsx` to:
1. Add a Meeting Mode hero card directly between the `.screen-header` block and the first `.hub-section` element (per D-02 and Pitfall 6 guidance — the hero card must NOT be inside a .hub-grid).
2. Replace the ACCOUNTABILITY section's two disabled cards with three enabled cards: KPI Management, Scorecard Oversight, and — wait, Meeting Mode is now hero. So the ACCOUNTABILITY section has only TWO cards: KPI Management + Scorecard Oversight. Meeting Mode lives ONLY in the hero position, not duplicated in the grid.

STEP 1 — Add the hero card markup AFTER the `<div className="status-summary">...</div>` block and BEFORE the first `<div className="hub-section">` block (the PARTNERS section). Wait — re-read the CONTEXT: D-02 says "promoted to a prominent hero card positioned above the existing section grid". Place the hero card directly after the status-summary (or after screen-header, whichever reads cleaner) and BEFORE the PARTNERS hub-section.

Insert this markup:

```jsx
<Link to="/admin/meeting" className="hub-card hub-card--hero" style={{ textDecoration: 'none' }}>
  <div className="hub-card-icon">{'\u{1F91D}'}</div>
  <h3>{copy.cards.meetingMode.title}</h3>
  <p>{copy.cards.meetingMode.description}</p>
</Link>
```

STEP 2 — In the ACCOUNTABILITY section (currently renders two `.hub-card.hub-card--disabled` divs for kpiManagement and meetingMode), REPLACE that block with two ENABLED Links:

```jsx
<div className="hub-section">
  <div className="eyebrow">{copy.sections.accountability}</div>
  <div className="hub-grid">
    <Link to="/admin/kpi" className="hub-card">
      <div className="hub-card-icon">{'\u{1F3AF}'}</div>
      <h3>{copy.cards.kpiManagement.title}</h3>
      <p>{copy.cards.kpiManagement.description}</p>
    </Link>
    <Link to="/admin/scorecards" className="hub-card">
      <div className="hub-card-icon">{'\u{1F4CB}'}</div>
      <h3>{copy.cards.scorecardOversight.title}</h3>
      <p>{copy.cards.scorecardOversight.description}</p>
    </Link>
  </div>
</div>
```

Notes:
- The Meeting Mode card from the old disabled grid is REMOVED entirely from the ACCOUNTABILITY section (it moved to hero position).
- The KPI Management disabled card is REPLACED with a live Link to /admin/kpi.
- A new Scorecard Oversight card is added, using the new `copy.cards.scorecardOversight` from content.js (added by P04-01).
- Both new Links remove the `hub-card--disabled` modifier and the `<span className="hub-card-disabled-label">` child.

DO NOT modify:
- The PARTNERS section (dashboard, partnerProfiles, comparison, test cards) — all preserved unchanged
- The status-summary block
- The loading/error/useEffect logic
- Any imports beyond what's needed (Link is already imported)

The file after changes should have: status-summary -> HERO Meeting Mode card -> PARTNERS section -> ACCOUNTABILITY section (2 enabled cards).
  </action>
  <verify>
    <automated>grep -q "hub-card--hero" src/components/admin/AdminHub.jsx && grep -q 'to="/admin/meeting"' src/components/admin/AdminHub.jsx && grep -q 'to="/admin/kpi"' src/components/admin/AdminHub.jsx && grep -q 'to="/admin/scorecards"' src/components/admin/AdminHub.jsx && grep -q "scorecardOversight" src/components/admin/AdminHub.jsx && ! grep -q "hub-card--disabled" src/components/admin/AdminHub.jsx && ! grep -q "hub-card-disabled-label" src/components/admin/AdminHub.jsx && npm run build 2>&1 | grep -q "built in"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "hub-card--hero" src/components/admin/AdminHub.jsx
    - grep finds Link with `to="/admin/meeting"` (hero card)
    - grep finds Link with `to="/admin/kpi"` (accountability card)
    - grep finds Link with `to="/admin/scorecards"` (accountability card)
    - grep -q "scorecardOversight" src/components/admin/AdminHub.jsx (references new copy key)
    - grep does NOT find "hub-card--disabled" anywhere (both old disabled cards are removed/replaced)
    - grep does NOT find "hub-card-disabled-label" (old disabled label span removed)
    - grep does NOT find "disabledLabel" (the now-removed copy field no longer referenced)
    - The PARTNERS hub-section still contains all four original cards (dashboard, partnerProfiles, comparison, test)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>AdminHub.jsx has Meeting Mode hero card above the section grid, KPI Management card enabled as Link to /admin/kpi, Scorecard Oversight card added as Link to /admin/scorecards, no disabled cards remaining in ACCOUNTABILITY, existing PARTNERS section unchanged. Build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Finalize ROADMAP.md Phase 4 checkboxes + status</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (find "Phase 4" section and Plans list; find Progress table)
    - .planning/phases/04-admin-tools-meeting-mode/04-01-SUMMARY.md
    - .planning/phases/04-admin-tools-meeting-mode/04-02-SUMMARY.md
    - .planning/phases/04-admin-tools-meeting-mode/04-03-SUMMARY.md
    - .planning/phases/04-admin-tools-meeting-mode/04-04-SUMMARY.md
  </read_first>
  <action>
Update `.planning/ROADMAP.md` to reflect Phase 4 completion.

STEP 1 — Update the top phase list:
Change:
  `- [ ] **Phase 4: Admin Tools & Meeting Mode** - Admin can manage all KPI data and facilitate structured Friday meetings`
To:
  `- [x] **Phase 4: Admin Tools & Meeting Mode** - Admin can manage all KPI data and facilitate structured Friday meetings`

STEP 2 — Update the Phase 4 Plans list. Replace:
  `**Plans**: TBD`
With:
```
**Plans:** 5 plans
Plans:
- [x] 04-01-PLAN.md — Schema migration 005 + supabase.js helpers + content.js COPY + CSS foundation
- [x] 04-02-PLAN.md — AdminKpi page (template CRUD + partner selections editor) + AdminPartners deep link
- [x] 04-03-PLAN.md — AdminPartners growth editor + KpiSelectionView extension + AdminScorecards page
- [x] 04-04-PLAN.md — AdminMeeting landing + AdminMeetingSession 10-stop wizard
- [x] 04-05-PLAN.md — App.jsx routes + AdminHub hero card + checklist finalization
```

STEP 3 — Update the Progress table at the bottom:
Change the Phase 4 row from:
  `| 4. Admin Tools & Meeting Mode | 0/TBD | Not started | - |`
To:
  `| 4. Admin Tools & Meeting Mode | 5/5 | Complete | {today's date in YYYY-MM-DD} |`

Use the current date from today's session (the executor should substitute the correct YYYY-MM-DD at execution time).

DO NOT modify other phase entries or the "Out of Scope" section.
  </action>
  <verify>
    <automated>grep -q "\\[x\\] \\*\\*Phase 4" .planning/ROADMAP.md && grep -q "04-01-PLAN.md" .planning/ROADMAP.md && grep -q "04-05-PLAN.md" .planning/ROADMAP.md && grep -q "5/5.*Complete" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ROADMAP.md contains `[x] **Phase 4: Admin Tools & Meeting Mode**` (checkbox checked)
    - Contains `04-01-PLAN.md` through `04-05-PLAN.md` references with `[x]` marks
    - Progress table row for Phase 4 shows `5/5` and `Complete`
    - Phase 1, 2, 3 checkbox entries unchanged
    - Out of Scope section unchanged
  </acceptance_criteria>
  <done>ROADMAP.md reflects Phase 4 as complete with 5/5 plans listed.</done>
</task>

</tasks>

<verification>
After all three tasks:

1. Build passes end-to-end:
```
npm run build
```

2. All Phase 4 routes registered:
```
grep -c "admin/kpi\|admin/scorecards\|admin/meeting" src/App.jsx
# Expected: at least 4
```

3. Hero card present, disabled cards removed:
```
grep -q "hub-card--hero" src/components/admin/AdminHub.jsx
! grep -q "hub-card--disabled" src/components/admin/AdminHub.jsx
```

4. Dev server sanity check (manual-ish — can be skipped if build passes):
```
# Start dev server, navigate to /admin/hub, confirm:
# - Hero Meeting Mode card appears at top
# - KPI Management + Scorecard Oversight cards appear below (enabled)
# - Clicking each navigates to the correct page without errors
```
</verification>

<success_criteria>
- All four Phase 4 routes registered in App.jsx (/admin/kpi, /admin/scorecards, /admin/meeting, /admin/meeting/:id)
- AdminHub shows Meeting Mode hero card above section grid
- AdminHub ACCOUNTABILITY section has KPI Management + Scorecard Oversight as enabled Links (no disabled cards, no Meeting Mode duplicate)
- ROADMAP.md Phase 4 marked complete with 5/5 plans listed
- npm run build passes
- Phase 4 is fully shippable: admin can navigate from hub to any new surface without 404s
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-tools-meeting-mode/04-05-SUMMARY.md` covering:
- Routes registered in App.jsx
- AdminHub structural changes (hero + enabled accountability cards)
- ROADMAP updates
- Confirmation that build passes end-to-end
- Any final deviations from the plan
- Notes on any remaining manual steps (e.g., Supabase migration application if not already done)
</output>
