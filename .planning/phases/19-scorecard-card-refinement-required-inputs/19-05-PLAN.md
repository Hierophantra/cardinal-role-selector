---
phase: 19-scorecard-card-refinement-required-inputs
plan: 05
type: execute
wave: 4
depends_on: ["19-01", "19-02", "19-03", "19-04"]
files_modified: []
autonomous: false
requirements: [REFINE-01, REFINE-02, REFINE-03, REFINE-04, REFINE-05, REFINE-06, REFINE-07, REFINE-08, REFINE-09, REFINE-10, REFINE-11, REFINE-12, REFINE-13, REFINE-14, REFINE-15]
must_haves:
  truths:
    - "Every REFINE-* requirement renders correctly in the test-partner scorecard editor"
    - "Every REFINE-* requirement renders correctly in the post-submit read-only mirror"
    - "Every REFINE-* requirement renders correctly in the AdminMeetingSession KpiStop for the partner who owns it (Theo or Jerry)"
    - "Every REFINE-* requirement renders correctly in the MeetingSummary KPI cell"
    - "Submit-gate behavior end-to-end matches REFINE-15 / D-04 / D-05 / D-06"
    - "Phase 19 Success Criterion 7 holds: pre-Phase-19 templates not edited by Phase 19 render byte-for-byte identically to Phase 18"
  artifacts: []
  key_links: []
---

<objective>
Wave 4: human-in-the-loop visual verification checkpoint. After Waves 0-3 have shipped (Wave 3 migration applied in Supabase), walk the test partner through every refined KPI card and confirm:
1. Editor renders the new key_fields shape correctly
2. Required-field gates fire as expected (disabled Submit + inline checklist)
3. Read-only mirrors (history, admin meeting, MeetingSummary) surface the new data shapes
4. No regression on unchanged templates (Phase 19 Success Criterion 7)

This wave makes NO code changes. It exists to gate phase-completion behind a human eyes-on review — Phase 19 is content-heavy and several decisions (multi_choice UI control, placeholder copy, helper-text wording) are reviewable only by looking at them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-scorecard-card-refinement-required-inputs/19-CONTEXT.md
@.planning/phases/19-scorecard-card-refinement-required-inputs/19-RESEARCH.md
@.planning/phases/19-scorecard-card-refinement-required-inputs/19-01-PLAN.md
@.planning/phases/19-scorecard-card-refinement-required-inputs/19-02-PLAN.md
@.planning/phases/19-scorecard-card-refinement-required-inputs/19-03-PLAN.md
@.planning/phases/19-scorecard-card-refinement-required-inputs/19-04-PLAN.md
@.planning/ROADMAP.md
@./CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Waves 0-3 of Phase 19 are complete:
- Wave 0 (19-01): Submit-gate machinery — extended validateStructuredFields (4 new schema features: hide_count, min_rows+shortfall_text, required_when, helperText), getValidationGaps + passesKeyFields + isRowAnswered + isRowSubmittable + findFirstMissingFieldAnchor helpers, deterministic input IDs, disabled-Submit button binding, inline missing-fields checklist with scrollIntoView anchor links, Phase 19 CSS appendix.
- Wave 1 (19-02): multi_choice field type — editor render (checkboxes for multi-select, radios for single_select), read-only render, substance.js structuredCompletion extension.
- Wave 2 (19-03): per-KPI "Reflection" rename to "Questions, Thoughts, or Concerns" everywhere it surfaces (5 string changes across 3 files).
- Wave 3 (19-04): Migration 026 applied in Supabase — 12 templates UPDATEd with refined key_fields, 1 template hard-DELETEd (cf7ec651), 4 reflection_prompts zeroed, 3 filter call sites verified (no `active` filter needed — D-17 honored verbatim).

Now we need human eyes on the editor + read-only renders + submit-gate behavior to confirm everything looks right.
  </what-built>
  <how-to-verify>

Open the Cardinal app in dev (`npm run dev`) and a Supabase SQL editor session.

### Step 1 — Open test-partner scorecard

Navigate to `/scorecard/test` (use `VITE_TEST_KEY` if prompted).

Confirm:
- [ ] Every REFINE-* affected KPI card renders without console errors.
- [ ] The "Reflection" label below each KPI now reads **"Questions, Thoughts, or Concerns"** (Wave 2, REFINE-01).
- [ ] The textarea placeholder reads **"Anything unclear, blocked, or worth raising? (optional)"**.
- [ ] No card shows the old "Reflection" label anywhere.
- [ ] The Weekly Reflection section at the bottom (tasks completed, win, learning, week rating) STILL reads "Weekly Reflection" — D-07 preserves this.

### Step 2 — Walk each refined card (editor view)

For each REFINE-* card listed below, visually confirm the editor renders the expected shape:

- [ ] **REFINE-02 (Meeting actionable idea, 0a24ffd6):** Single required text field with label "One actionable idea, observation, or challenge that moves the conversation forward". No count input, no noteworthy list.
- [ ] **REFINE-03 (Reach out to team members, 7bd0bb5f):** row_per_item; no count input visible (hide_count); each row has two required text inputs (Name + intentional check-in signal).
- [ ] **REFINE-04 (Theo outreach, 13dc13fe):** Noteworthy structure preserved; no inline count input on any row; no week-total counter at the bottom of the card; card header reads "Outreach actions (e.g. text, call, in-person, email)".
- [ ] **REFINE-05 + REFINE-08 (Jerry Friday Financial, f8420dfb):** Merged card with major_expenses row_list (helper text "$1500+, not contractor payments"), four currency fields (total_expenses, pending_estimates, projected_revenue, outstanding_invoices_total), financial_notes textarea with placeholder "e.g. upcoming payment deadlines". Old discrepancy/discrepancy_explanation fields ABSENT.
- [ ] **REFINE-06 (Jerry research, 9f372633):** multi-select chips (checkboxes) with 5 options (competitor / certification / award / new standard / other). Selecting two options surfaces two per-selection sub-blocks each with required "What you found" + "Next steps" text inputs. Selecting/deselecting works correctly.
- [ ] **REFINE-07 (Jerry social media, 30a07161):** yes_no required + conditional details text. Picking "No" does NOT show the details field. Picking "Yes" shows the required details text input. No mention of "social media consultant" anywhere on the card.
- [ ] **REFINE-09 (Theo BD actions, 7544e86b):** No inline count input on any row; no total-count below; 2 noteworthy rows pre-rendered with an "add another" affordance.
- [ ] **REFINE-10 (Theo coaching, 2c51fe62):** Three required text fields (who_coached, focus_area, how_long). All marked with red asterisk `*`.
- [ ] **REFINE-11 (Theo delegation, aa47eb25):** Three required text fields (delegated_to, what_delegated, current_result). All marked required.
- [ ] **REFINE-12 (Jerry outstanding invoices, 172b5023):** row_per_item with no count input; helper "If fewer than 3 follow-ups — why?" text input surfaces when fewer than 3 rows are entered. Adding a 3rd row hides the shortfall prompt. Adding rows beyond 3 works without complaint.
- [ ] **REFINE-13 (Jerry gross margin, 403778b7):** Helper text directly under the structured block title reads "Gross Margin = (Revenue − COGS) / Revenue". row_per_item with no count input. Each row has job_id (text required) + gross_margin (currency required, $ prefix renders).
- [ ] **REFINE-14 (Jerry operational process, 9c39ff9a):** Three radio buttons (Documented / Updated / Improved) — single-select. Required description text field below. Cannot mark Yes without both populated.

### Step 3 — Soft-retire verification

Navigate to `/weekly-kpi/theo`.

- [ ] The optional KPI pool does NOT show "Brief summary of expected closings" (cf7ec651 soft-retired).
- [ ] Other Theo optional KPIs (BD actions, coaching, delegation) still appear in the pool.

Confirm in Supabase:
- [ ] `SELECT id, active FROM kpi_templates WHERE active = false;` returns exactly one row (cf7ec651).

### Step 4 — Submit-gate behavior (REFINE-15 + D-04 + D-05 + D-06)

Back on `/scorecard/test`:

- [ ] With NO week rating selected, the sticky-bar Submit button is visually disabled (greyed).
- [ ] An inline checklist above the sticky bar lists missing items. The first item is "Overall week rating (1-5)".
- [ ] Click the "Overall week rating (1-5)" link — the page scrolls smoothly to the week-rating control.
- [ ] Select a rating. The "Overall week rating (1-5)" item disappears from the checklist.
- [ ] Mark a coaching/delegation/gross-margin/operational-process KPI as Yes WITHOUT populating its required structured fields. The Submit button remains disabled. The checklist shows that KPI as a gap. Clicking the gap link scrolls to the first missing field on the row.
- [ ] Populate all required fields. The checklist clears. The Submit button enables. Click Submit → existing confirmation modal opens (untouched by Phase 19).
- [ ] On the outstanding-invoices KPI, enter only 2 rows and leave the shortfall_text empty. The row is treated as not yet validated; Submit stays disabled with the gap pointing at the shortfall_text input.
- [ ] Fill the shortfall_text. The gap clears.

### Step 5 — Read-only mirrors

Submit the scorecard for the test partner.

- [ ] Reload `/scorecard/test`. The post-submit read-only mirror surfaces every refined card's structured data correctly (multi_choice selections show as a list with per-selection field values; helperText renders italic muted under each structured block; shortfall_text surfaces where applicable).

Open the admin app:
- [ ] Navigate to a Friday meeting session for the test partner (or trigger via meeting flow). For each KpiStop, the read-only structured-data block matches the post-submit mirror.

Navigate to a meeting summary (after closing or stepping through a meeting):
- [ ] The KPI cells in MeetingSummary render the same structured-data shapes — confirmed because all three surfaces use the same `StructuredFieldsReadOnly` component (RESEARCH Topic 1).

### Step 6 — Regression check (Phase 19 Success Criterion 7)

Open a partner whose templates were NOT edited by Phase 19 — e.g., Theo M2 (active job check-ins, 8a67b59f), Theo M3 (Acculynx data entry, 438e779e), Theo M4 (closing rate, f1ad9c7d), Jerry M2 (post-job client experience, d59c1c56), Jerry C1 (conditional sales, 50790c0d):

- [ ] Each of these cards renders byte-for-byte identically to its Phase 18 form (same fields, same labels, same inputs).
- [ ] The per-KPI textarea now reads "Questions, Thoughts, or Concerns" instead of "Reflection" (this IS the intended Wave 2 change — applies to ALL templates including unedited ones because the label comes from SCORECARD_COPY.reflectionLabel).

### Step 7 — Documentation surface

- [ ] Open the Phase 19 SUMMARY files (19-01, 19-02, 19-03, 19-04). Each summary reads cleanly and references the artifacts produced.
- [ ] Read `.planning/ROADMAP.md` — Phase 19 entry's plan list (when finalized by the orchestrator) accurately reflects 5 plans.

  </how-to-verify>
  <resume-signal>
Type "approved" when all checkboxes above pass. If anything fails, describe what you saw — the planner will produce a follow-up `--gaps` plan if needed.
  </resume-signal>
</task>

</tasks>

<verification>
This wave is a human-verification checkpoint. There is no automated verification beyond the per-step checkboxes in the task above. Upon human approval, Phase 19 is complete and ready for retrospective.

If the human flags any gap (failed checkbox), the orchestrator should:
1. Capture the gap description in `.planning/phases/19-scorecard-card-refinement-required-inputs/19-VERIFICATION.md`
2. Run `/gsd-plan-phase 19 --gaps` to produce gap-closure plans
3. Re-run Wave 4 verification after closure
</verification>

<success_criteria>
- [ ] All 6 step checkpoints in Step 1-6 are checked off by the human
- [ ] Phase 19 Success Criterion 1: rename surfaces in every render path (editor + 3 read-only renderers) — verified in Step 1 + Step 5
- [ ] Phase 19 Success Criterion 2: weekRating gate blocks submit with inline pointer — verified in Step 4
- [ ] Phase 19 Success Criterion 3: required structured fields gate (focused-time, delegation, gross-margin, operational-process) — verified in Step 4
- [ ] Phase 19 Success Criterion 4: financial KPI inputs render with currency formatting in editor + admin views in consistent order — verified in Step 2 (REFINE-05+08) and Step 5
- [ ] Phase 19 Success Criterion 5: outstanding-invoices min-3 with shortfall justification — verified in Step 2 (REFINE-12) and Step 4
- [ ] Phase 19 Success Criterion 6: research KPI multi-select with per-selection fields surfaces in admin views — verified in Step 2 (REFINE-06) and Step 5
- [ ] Phase 19 Success Criterion 7: no regression on unchanged templates — verified in Step 6
</success_criteria>

<output>
After human approval, create `.planning/phases/19-scorecard-card-refinement-required-inputs/19-05-SUMMARY.md` recording:
- Verification outcome (approved or gap list)
- Any noted UX observations during walk-through (helper-text wording feedback, multi_choice control feedback, placeholder copy reception)
- Phase 19 declared complete OR gap-closure recommended (with specific items)
</output>

<phase_goal_backward_traceability>

## Phase 19 Success Criteria → Wave / Plan Mapping

Sourced from `.planning/ROADMAP.md` Phase 19 entry (lines 147-155).

| Success Criterion | Wave / Plan | Verification |
|------|------|------|
| 1. Every per-KPI structured row reads "Questions, Thoughts, or Concerns" in editor + all read-only renderers | Wave 2 (19-03) | Wave 4 Step 1 + Step 5 |
| 2. Partner cannot submit without week rating; inline pointer surfaces | Wave 0 (19-01) | Wave 4 Step 4 |
| 3. Required structured fields gate Yes-rating for focused-time / delegation / gross-margin / operational-process | Wave 0 validation + Wave 3 schema | Wave 4 Step 2 + Step 4 |
| 4. Financial KPI inputs render with currency formatting in consistent order | Wave 3 migration (REFINE-05/08) | Wave 4 Step 2 + Step 5 |
| 5. Outstanding-invoices min-3 + shortfall justification persists | Wave 0 validation + Wave 3 schema | Wave 4 Step 2 + Step 4 |
| 6. Research KPI multi-select + per-selection fields surface in admin views | Wave 1 multi_choice render + Wave 3 schema | Wave 4 Step 2 + Step 5 |
| 7. No regression on pre-Phase-19 templates not edited by Phase 19 | All waves (additive contract) | Wave 4 Step 6 |

## REFINE-01..15 → Wave / Plan Mapping

| Requirement | Wave / Plan | Where Implemented |
|------|------|------|
| REFINE-01 (rename per-KPI "Reflection") | Wave 2 (19-03) | content.js reflectionLabel + 2 hardcoded placeholders in AdminMeetingSession* |
| REFINE-02 (Meeting actionable idea card) | Wave 3 (19-04) | Migration 026 SECTION 4 (0a24ffd6) |
| REFINE-03 (Team-member reach-out lightweight check-in) | Wave 3 (19-04) | Migration 026 SECTION 5 (7bd0bb5f) — row_per_item + min_rows:1 + hide_count |
| REFINE-04 (10+ outreach: drop count + counter) | Wave 0 (hide_count semantics) + Wave 3 (19-04) | Migration 026 SECTION 6 (13dc13fe) — adds hide_count flag |
| REFINE-05 (Friday financial Major Expenses ≥$1500 + total + notes) | Wave 3 (19-04) | Migration 026 SECTION 3 (f8420dfb merged shape) |
| REFINE-06 (Research multi-choice category + per-selection fields) | Wave 1 (19-02) + Wave 3 (19-04) | multi_choice render in 19-02; schema in Migration 026 SECTION 11 (9f372633) |
| REFINE-07 (Social media: yes_no + conditional details) | Wave 0 (required_when validation) + Wave 3 (19-04) | Migration 026 SECTION 10 (30a07161) |
| REFINE-08 (Expected closings folded into Friday financial) | Wave 3 (19-04) | Migration 026 SECTION 2 soft-retire cf7ec651 + SECTION 3 merged shape |
| REFINE-09 (BD actions: drop count + below-total) | Wave 0 (hide_count semantics) + Wave 3 (19-04) | Migration 026 SECTION 7 (7544e86b) |
| REFINE-10 (Focused-time coaching: 3 required fields) | Wave 3 (19-04) | Migration 026 SECTION 8 (2c51fe62) |
| REFINE-11 (Delegation: 3 required fields) | Wave 3 (19-04) | Migration 026 SECTION 9 (aa47eb25) |
| REFINE-12 (Outstanding invoices: min 3 + why justification) | Wave 0 (min_rows + shortfall_text validation) + Wave 3 (19-04) | Migration 026 SECTION 14 (172b5023) |
| REFINE-13 (Gross margin: job + value + equation in helper text) | Wave 0 (helperText render) + Wave 3 (19-04) | Migration 026 SECTION 12 (403778b7) — currency + helperText + hide_count |
| REFINE-14 (Operational process: single-select + required text) | Wave 1 (single_select multi_choice render) + Wave 3 (19-04) | Migration 026 SECTION 13 (9c39ff9a) |
| REFINE-15 (Week rating hard required to submit) | Wave 0 (19-01) | Scorecard.jsx handleSubmit week-rating gate + getValidationGaps |

## Coverage Audit

- REQ source (REQUIREMENTS.md REFINE-01..15): 15 requirements → 15 covered, 0 missing
- GOAL source (ROADMAP Phase 19 Success Criteria 1-7): 7 criteria → 7 covered, 0 missing
- CONTEXT source (CONTEXT.md D-01..D-17 locked decisions): 17 decisions → all implemented (D-01 Wave 3 § 2-3; D-02 Wave 3 § 3 dropped fields; D-03 Wave 0 validation + Wave 1 render + Wave 3 emit; D-04 Wave 0 disabled-Submit + checklist; D-05 Wave 0 passesKeyFields/isRowAnswered/isRowSubmittable helper trio (Yes-but-invalid narrow scope; Pending NEVER counts toward counter pill); D-06 Wave 0 min_rows + shortfall_text + Wave 3 § 14; D-07 Wave 2; D-08..D-11 Wave 3 § 8/9/12/13; D-12..D-16 Wave 3 § 4/5/6/10/7; D-17 Wave 3 honored verbatim — hard-DELETE cf7ec651, zero DDL per RESEARCH Topic 13 Q6 path (b))
- RESEARCH source: every choice point in RESEARCH.md Topic 13 has a planner resolution in the appropriate wave's `<resolved_choice_points>` block

No deferred items from CONTEXT.md `<deferred>` are scheduled. No items scoped to other phases. Single-phase coverage complete.

</phase_goal_backward_traceability>
