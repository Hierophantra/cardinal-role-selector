---
phase: 19-scorecard-card-refinement-required-inputs
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/data/content.js
  - src/components/admin/AdminMeetingSession.jsx
  - src/components/admin/AdminMeetingSessionMock.jsx
autonomous: true
requirements: [REFINE-01]
must_haves:
  truths:
    - "SCORECARD_COPY.reflectionLabel reads 'Questions, Thoughts, or Concerns' (NOT 'Reflection')"
    - "SCORECARD_COPY.reflectionPlaceholder is a non-empty Cardinal-voice placeholder string consistent with the new framing"
    - "SCORECARD_COPY.submitErrorReflectionRequired references 'Questions, Thoughts, or Concerns' wording (NOT 'reflection')"
    - "AdminMeetingSession.jsx Trace-override textarea uses placeholder='Questions, Thoughts, or Concerns...' (NOT 'Reflection...')"
    - "AdminMeetingSessionMock.jsx Trace-override textarea uses placeholder='Questions, Thoughts, or Concerns...' (NOT 'Reflection...')"
    - "Weekly Reflection block constants (reflectionEyebrow at line 512; weeklyReflectionHeading at line 530) are UNCHANGED — D-07 scope excludes the weekly summary block"
  artifacts:
    - path: "src/data/content.js"
      provides: "reflectionLabel rename + reflectionPlaceholder copy + submitErrorReflectionRequired rephrase"
      contains: "'Questions, Thoughts, or Concerns'"
    - path: "src/components/admin/AdminMeetingSession.jsx"
      provides: "hardcoded placeholder string rename at line ~1701"
      contains: "Questions, Thoughts, or Concerns"
    - path: "src/components/admin/AdminMeetingSessionMock.jsx"
      provides: "hardcoded placeholder string rename at line ~651"
      contains: "Questions, Thoughts, or Concerns"
  key_links:
    - from: "Scorecard.jsx per-KPI reflection label"
      to: "SCORECARD_COPY.reflectionLabel"
      via: "import + JSX text"
      pattern: "SCORECARD_COPY\\.reflectionLabel"
    - from: "Scorecard.jsx per-KPI reflection textarea"
      to: "SCORECARD_COPY.reflectionPlaceholder"
      via: "placeholder prop"
      pattern: "SCORECARD_COPY\\.reflectionPlaceholder"
---

<objective>
Wave 2: rename the per-KPI "Reflection" prompt to "Questions, Thoughts, or Concerns" everywhere it surfaces (REFINE-01, D-07). This is a content-only wave — no component logic changes, no schema, no migration. Three files modified.

Purpose: D-07 narrowly scopes the rename to the per-KPI prompt — the Weekly Reflection summary block at the bottom of the scorecard (tasks completed, win, learning, week rating) keeps its existing "Weekly Reflection" header because it's a week summary, not a per-KPI question prompt.

Output:
- `SCORECARD_COPY.reflectionLabel` set to `'Questions, Thoughts, or Concerns'`
- `SCORECARD_COPY.reflectionPlaceholder` set to a Cardinal-voice placeholder string
- `SCORECARD_COPY.submitErrorReflectionRequired` rephrased to use the new framing (RESEARCH Topic 13 Q5 — planner picks consistency)
- AdminMeetingSession.jsx hardcoded "Reflection..." placeholder string renamed
- AdminMeetingSessionMock.jsx hardcoded "Reflection..." placeholder string renamed

Wave 2 is independent of Wave 0 and Wave 1 (no dependency on multi_choice or validation extensions). It is scheduled in Wave 2 only to keep diff hygiene clean — content-rename PRs are easier to review when isolated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-scorecard-card-refinement-required-inputs/19-CONTEXT.md
@.planning/phases/19-scorecard-card-refinement-required-inputs/19-RESEARCH.md
@./CLAUDE.md

@src/data/content.js
@src/components/admin/AdminMeetingSession.jsx
@src/components/admin/AdminMeetingSessionMock.jsx

<interfaces>
<!-- Exact line anchors per RESEARCH.md Topic 4. -->

src/data/content.js current state:
- Line 512: `reflectionEyebrow: 'Weekly Reflection',`  — DO NOT change (weekly block, not per-KPI)
- Line 528: `reflectionLabel: 'Reflection',`  — CHANGE to `'Questions, Thoughts, or Concerns'`
- Line 529: `reflectionPlaceholder: '',`  — CHANGE to non-empty placeholder (planner picks)
- Line 530: `weeklyReflectionHeading: 'Weekly Reflection',`  — DO NOT change (weekly block, not per-KPI)
- Lines 541-542: `submitErrorReflectionRequired: 'Add a reflection to every KPI before submitting. Every Yes, No, and Pending row needs your evidence.',`  — REPHRASE per RESEARCH Q5

src/components/admin/AdminMeetingSession.jsx:
- Line ~1701: literal `placeholder="Reflection..."` on the Trace override textarea — CHANGE the literal string.

src/components/admin/AdminMeetingSessionMock.jsx:
- Line ~651: literal `placeholder="Reflection..."` on the Trace override textarea (mock variant) — CHANGE the literal string.

</interfaces>

<resolved_choice_points>
- **C5 (rephrase submitErrorReflectionRequired):** YES — rewrite to use "Questions, Thoughts, or Concerns" framing for consistency. The error message exists to direct the partner back to the rename target; saying "Add a reflection..." after the rename would be jarring.
- **C8 (placeholder copy):** `"Anything unclear, blocked, or worth raising? (optional)"`. Cardinal voice match to neighboring placeholders (cf. content.js:514 "What did you get done? (optional)", :516 "What's moving to next week? (optional)", :521 "What did you learn? (optional)").
</resolved_choice_points>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename SCORECARD_COPY.reflectionLabel + placeholder + error string in src/data/content.js</name>
  <files>src/data/content.js</files>
  <read_first>
    - src/data/content.js lines 487-606 (full SCORECARD_COPY object — confirm Phase 19 keys from Wave 0 plan 19-01 are intact)
    - Lines 512, 528, 529, 530, 541-542 specifically (the rename targets vs the leave-alone Weekly Reflection block keys)
  </read_first>
  <action>
Make three surgical string changes inside `SCORECARD_COPY` in `src/data/content.js`.

**Change 1 — Line 528 `reflectionLabel`:**
From:
```js
  reflectionLabel: 'Reflection',
```
To:
```js
  reflectionLabel: 'Questions, Thoughts, or Concerns',
```

**Change 2 — Line 529 `reflectionPlaceholder`:**
From:
```js
  reflectionPlaceholder: '',
```
To:
```js
  reflectionPlaceholder: 'Anything unclear, blocked, or worth raising? (optional)',
```

**Change 3 — Lines 541-542 `submitErrorReflectionRequired`:**
From:
```js
  submitErrorReflectionRequired:
    'Add a reflection to every KPI before submitting. Every Yes, No, and Pending row needs your evidence.',
```
To:
```js
  submitErrorReflectionRequired:
    'Add a Questions, Thoughts, or Concerns response to every KPI before submitting. Every Yes, No, and Pending row needs your evidence.',
```

DO NOT change any other keys. Specifically, DO NOT change:
- Line 512: `reflectionEyebrow: 'Weekly Reflection',`  (weekly block)
- Line 530: `weeklyReflectionHeading: 'Weekly Reflection',`  (weekly block)
- Lines 785-794: `MEETING_COPY.stops.weeklyReflectionReview*` (Friday meeting stop, different concept)
- Phase 19 Wave 0 keys added in plan 19-01 (`submitErrorWeekRatingRequired`, `submitErrorShortfallRequired`, `submitChecklistEyebrow`, `submitChecklistEmpty`)
  </action>
  <verify>
    <automated>node -e "import('./src/data/content.js').then(m => { const c = m.SCORECARD_COPY; const failures = []; if (c.reflectionLabel !== 'Questions, Thoughts, or Concerns') failures.push('reflectionLabel='+JSON.stringify(c.reflectionLabel)); if (c.reflectionPlaceholder !== 'Anything unclear, blocked, or worth raising? (optional)') failures.push('reflectionPlaceholder='+JSON.stringify(c.reflectionPlaceholder)); if (!c.submitErrorReflectionRequired || !c.submitErrorReflectionRequired.includes('Questions, Thoughts, or Concerns')) failures.push('submitErrorReflectionRequired missing new framing'); if (c.reflectionEyebrow !== 'Weekly Reflection') failures.push('reflectionEyebrow REGRESSED (must remain Weekly Reflection per D-07)'); if (c.weeklyReflectionHeading !== 'Weekly Reflection') failures.push('weeklyReflectionHeading REGRESSED'); if (failures.length) { console.error('FAIL:', failures.join('; ')); process.exit(1); } console.log('OK'); })"</automated>
    Additional grep checks:
    - `grep -v '^#' src/data/content.js | grep -c "reflectionLabel: 'Questions, Thoughts, or Concerns'"` == 1
    - `grep -v '^#' src/data/content.js | grep -c "reflectionLabel: 'Reflection'"` == 0  (old value fully removed)
    - `grep -v '^#' src/data/content.js | grep -c "reflectionEyebrow: 'Weekly Reflection'"` == 1  (preserved)
    - `grep -v '^#' src/data/content.js | grep -c "weeklyReflectionHeading: 'Weekly Reflection'"` == 1  (preserved)
  </verify>
  <done>
    SCORECARD_COPY.reflectionLabel renamed; reflectionPlaceholder set to non-empty Cardinal-voice copy; submitErrorReflectionRequired rephrased; Weekly Reflection block constants untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename hardcoded "Reflection..." placeholder in AdminMeetingSession.jsx</name>
  <files>src/components/admin/AdminMeetingSession.jsx</files>
  <read_first>
    - src/components/admin/AdminMeetingSession.jsx around line 1701 (verify the exact line with `grep -n 'placeholder="Reflection' src/components/admin/AdminMeetingSession.jsx` first — line numbers may drift slightly if any prior wave was committed first)
    - RESEARCH.md Topic 4 Pitfall 9 (these two files were the only ones with hardcoded `placeholder="Reflection..."` outside content.js)
  </read_first>
  <action>
In `src/components/admin/AdminMeetingSession.jsx`, locate the line containing `placeholder="Reflection..."` (RESEARCH identifies this at ~line 1701; verify by grep first).

Change:
```jsx
placeholder="Reflection..."
```

To:
```jsx
placeholder="Questions, Thoughts, or Concerns..."
```

There should be ONLY ONE occurrence in this file. If grep finds more than one, surface that as a discrepancy with RESEARCH before changing — RESEARCH explicitly verified one occurrence per file.

DO NOT extract the literal into `SCORECARD_COPY.reflectionPlaceholder` even though the content matches conceptually. This is the Trace override textarea (admin-facing), which uses an ellipsis suffix for terseness — different from the partner-facing placeholder. Keeping the literal here avoids introducing a new content.js key just for the ellipsis variant. If a future cleanup phase consolidates, that's a discretion-level refactor — out of scope for Phase 19.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -i "error\|fail" | head -20</automated>
    Expected: no error / fail lines.
    Additional grep checks:
    - `grep -c 'placeholder="Reflection\\.\\.\\."' src/components/admin/AdminMeetingSession.jsx` == 0  (old string fully removed)
    - `grep -c 'placeholder="Questions, Thoughts, or Concerns\\.\\.\\."' src/components/admin/AdminMeetingSession.jsx` == 1
  </verify>
  <done>
    The single hardcoded "Reflection..." placeholder string in AdminMeetingSession.jsx is replaced with "Questions, Thoughts, or Concerns..."; build passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rename hardcoded "Reflection..." placeholder in AdminMeetingSessionMock.jsx</name>
  <files>src/components/admin/AdminMeetingSessionMock.jsx</files>
  <read_first>
    - src/components/admin/AdminMeetingSessionMock.jsx around line 651 (verify by grep first)
  </read_first>
  <action>
Same change as Task 2, in the mock variant file. Locate the line containing `placeholder="Reflection..."` (RESEARCH identifies this at ~line 651).

Change:
```jsx
placeholder="Reflection..."
```

To:
```jsx
placeholder="Questions, Thoughts, or Concerns..."
```

If grep finds more than one occurrence, surface that as a discrepancy.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -i "error\|fail" | head -20</automated>
    Additional grep checks:
    - `grep -c 'placeholder="Reflection\\.\\.\\."' src/components/admin/AdminMeetingSessionMock.jsx` == 0
    - `grep -c 'placeholder="Questions, Thoughts, or Concerns\\.\\.\\."' src/components/admin/AdminMeetingSessionMock.jsx` == 1
  </verify>
  <done>
    The single hardcoded "Reflection..." placeholder string in AdminMeetingSessionMock.jsx is replaced with "Questions, Thoughts, or Concerns..."; build passes.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Build:** `npm run build` succeeds with no errors.
2. **Codebase rename audit:**
   - `grep -nri 'placeholder="Reflection' src/` returns ZERO matches.
   - `grep -nri "reflectionLabel: 'Reflection'" src/` returns ZERO matches.
3. **Partner-facing label visible:** Open `/scorecard/test` in dev. Each KPI row's per-KPI textarea has the new label "Questions, Thoughts, or Concerns" and placeholder "Anything unclear, blocked, or worth raising? (optional)".
4. **Submit error message rephrased:** Trigger the submit-error path (leave one row's reflection empty, click Submit when Wave 0 hasn't shipped yet OR via Wave 0's checklist). Confirm the error/checklist surfaces the new framing.
5. **Weekly Reflection block UNCHANGED:** Scrolling to the bottom of the scorecard, the section header still reads "Weekly Reflection" — and the existing tasks-completed / win / learning / week rating fields read their original labels.
6. **Friday meeting copy unchanged:** Open a Friday meeting in admin. The `weeklyReflectionReview*` stop reads its original copy (MEETING_COPY.stops.weeklyReflectionReview*) — RESEARCH confirms this is a separate block.
7. **AdminMeetingSession Trace override:** Open a Friday meeting in admin, navigate to a KpiStop. The Trace override textarea placeholder reads "Questions, Thoughts, or Concerns..." (with ellipsis).

</verification>

<success_criteria>
- [ ] SCORECARD_COPY.reflectionLabel is exactly 'Questions, Thoughts, or Concerns'
- [ ] SCORECARD_COPY.reflectionPlaceholder is exactly 'Anything unclear, blocked, or worth raising? (optional)'
- [ ] SCORECARD_COPY.submitErrorReflectionRequired contains the literal substring 'Questions, Thoughts, or Concerns'
- [ ] SCORECARD_COPY.reflectionEyebrow remains 'Weekly Reflection' (D-07 explicitly excludes this)
- [ ] SCORECARD_COPY.weeklyReflectionHeading remains 'Weekly Reflection'
- [ ] AdminMeetingSession.jsx line ~1701 placeholder is now "Questions, Thoughts, or Concerns..."
- [ ] AdminMeetingSessionMock.jsx line ~651 placeholder is now "Questions, Thoughts, or Concerns..."
- [ ] grep -nri 'placeholder="Reflection' src/ returns zero matches
- [ ] npm run build passes
- [ ] No file modified outside the three listed in files_modified
</success_criteria>

<output>
After completion, create `.planning/phases/19-scorecard-card-refinement-required-inputs/19-03-SUMMARY.md` documenting:
- 3 SCORECARD_COPY edits + 2 hardcoded placeholder edits = 5 string changes total
- Weekly Reflection block constants explicitly preserved per D-07
- Trace override placeholder kept as a literal (not extracted to content.js) — minor discretion call documented for future cleanup phase consideration
</output>
