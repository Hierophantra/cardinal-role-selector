---
phase: 11
slug: season-overview-progress
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-13
---

# Phase 11 — UI Design Contract

> Visual and interaction contract for Season Overview & Progress.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — vanilla CSS, project-defined classes |
| Icon library | Unicode literals inline (arrow →, bullets) — no icon library |
| Font | DM Sans, system-ui, -apple-system, Segoe UI, sans-serif (from index.css :root) |

No `components.json` found. Project uses vanilla CSS with a Cardinal dark theme. shadcn is not applicable — project constraint is React 18 + Vite + vanilla CSS (CLAUDE.md).

---

## Spacing Scale

Declared values (multiples of 4, sourced from index.css patterns):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, dot spacing, badge internal padding |
| sm | 8px | Gap between hub card elements (gap: 12px in .hub-card is exception, noted below) |
| md | 16px | Hub grid gap, section padding, chart bar vertical gap |
| lg | 24px | Hub grid margin-top, card padding (hub-card: 24px) |
| xl | 32px | Section bottom margin (.hub-section), page-level section breaks |
| 2xl | 48px | Major section separation on progress page |
| 3xl | 64px | Not used in this phase |

Exceptions:
- `.hub-card` uses `gap: 12px` between internal elements (existing pattern, preserved)
- `.hub-card--hero` uses `padding: 32px` (existing pattern, not used in this phase)
- Bar chart row height: 32px per KPI bar (accommodates label + bar in single row)
- Sparkline mini bars on hub card: 6px tall, 4px gap between bars

---

## Typography

Matches existing index.css scale — no new sizes introduced.

| Role | Size | Weight | Line Height | Usage in this phase |
|------|------|--------|-------------|---------------------|
| Body | 15px | 400 | 1.55 | Hub card description, growth priority note text, chart tooltip |
| Label | 12px | 700 | 1.3 | Eyebrow text (uppercase, 0.18em letter-spacing), miss streak badge, sparkline percentage labels, growth priority group label |
| Heading | 20px | 700 | 1.3 | Hub card h3 ("Season Overview"), progress page section headings |
| Display | 28px | 700 | 1.2 | Season hit-rate percentage shown prominently on progress page overview section |

Additional detail:
- Hit-rate % on hub card: 15px, weight 700, color `var(--text)` — matches `.scorecard-hit-rate` pattern
- "Week N of ~26" on hub card: 12px, weight 400, color `var(--muted)`
- KPI label on bar chart left axis: 14px, weight 400, color `var(--muted)` — truncate at 28ch with ellipsis
- Bar chart percentage annotations: 12px, weight 700, color `var(--text)`
- Growth priority card status badge: 12px, weight 700, uppercase

---

## Color

Existing CSS custom properties from index.css `:root` — no new tokens added.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--bg)` = #141414 | Page background (applied via html radial gradient) |
| Secondary (30%) | `var(--surface)` = #1E1E1E | Hub card background, progress page section cards, growth priority cards |
| Accent (10%) | `var(--red)` = #C41E3A | Hub card icon background tint (rgba 10%), hub card border-left if season has active miss streak |
| Gold label | `var(--gold)` = #D4A843 | Eyebrow labels, season name label, moderate-performance bar fill (50-79% hit-rate) |
| Success | `var(--success)` = #2D8F5E | High-performance bar fill (80%+ hit-rate), hit-rate % text when above 80% |
| Miss/Warning | `var(--miss)` = #C41E3A | Low-performance bar fill (<50% hit-rate), hit-rate % text when below 50% |
| Text | `var(--text)` = #FAFAFA | Primary readable content |
| Muted | `var(--muted)` = #AAAAAA | Secondary text, week progress line, chart axis labels |
| Border | `var(--border)` = #2e2e2e | Card borders, section dividers |

Accent (`var(--red)`) reserved for:
- Hub card `.hub-card-icon` background tint (rgba(196, 30, 58, 0.10)) — follows existing hub card icon pattern
- Left border stripe on hub card only if partner has an active miss streak (border-left: 3px solid var(--red)) — mirrors `.status-summary` pattern
- No other elements in this phase use `var(--red)` as accent

Performance gradient color coding (D-07 from CONTEXT.md):
- 80%+ hit-rate: `var(--success)` #2D8F5E (green)
- 50-79% hit-rate: `var(--gold)` #D4A843 (amber)
- <50% hit-rate: `var(--miss)` #C41E3A (red)

No destructive actions exist in this phase.

---

## Component Inventory

### 1. Season Overview Hub Card (new, in PartnerHub.jsx)

Class: `.hub-card` (existing) — inserted as first child of `.hub-grid`

Visibility gate: `kpiLocked && <SeasonOverviewCard />` — consistent with Scorecard and Meeting History cards (D-03)

Internal layout (flex column, gap 12px per `.hub-card`):
- Eyebrow: `<span className="eyebrow">SEASON OVERVIEW</span>` — 12px, uppercase, gold
- Heading h3: "Season Overview" — 20px, weight 700
- Hit-rate line: `<span className="progress-hit-rate">[NN]% this season</span>` — 15px, weight 700, color driven by performance threshold
- Week progress line: `<span className="progress-week-label">Week N of ~26</span>` — 12px, weight 400, muted
- Worst streak alert (conditional): `<span className="progress-streak-alert">[KPI name]: missed N weeks</span>` — 12px, weight 400, muted-2. Omitted entirely if no active streak.
- Mini sparklines: `<div className="progress-sparklines">` containing one `<div className="progress-sparkline-bar">` per KPI, width set inline as hit-rate percentage, color set inline per performance threshold, height 6px, border-radius 3px
- CTA: `<span className="hub-card-cta">View season progress →</span>` — inherits existing hub card CTA styling

### 2. Partner Progress Page (new component: PartnerProgress.jsx)

Route: `/progress/:partner` — consistent with `/scorecard/:partner` and `/meeting-history/:partner` (D-13)

Page layout: `.app-shell > .container > .screen.fade-in`

Back nav: `<Link to="/hub/:partner" className="btn-ghost">← Back to Hub</Link>` in `.nav-row`

Framer Motion: `motionProps = { initial: { opacity: 0, y: 8 }, animate: { opacity: 1, y: 0 }, exit: { opacity: 0, y: -8 }, transition: { duration: 0.28, ease: 'easeOut' } }` — same as Scorecard.jsx pattern

Section order (D-14):
1. **Season Overview header** — eyebrow + display hit-rate + week progress + season name
2. **Per-KPI bar chart** — recharts `BarChart`, horizontal layout (D-06), miss streak badges inline
3. **Growth priority status cards** — one card per priority

### 3. Season Overview Section (top of progress page)

Class: `.progress-overview` — `background: var(--surface); border: 1px solid var(--border); border-radius: 16px; padding: 24px; margin-bottom: 24px`

Elements:
- Eyebrow: `<span className="eyebrow" style={{ color: 'var(--gold)' }}>SPRING SEASON 2026</span>`
- Display stat: `<div className="progress-stat-display"><span className="progress-stat-value">[NN]%</span><span className="progress-stat-label">season hit rate</span></div>` — value is 28px weight 700, color driven by threshold; label is 12px weight 400 muted
- Week indicator: `<div className="progress-week-indicator">Week N of ~26</div>` — 15px weight 400

### 4. Per-KPI Bar Chart (recharts, progress page)

Component: recharts `BarChart` with `layout="vertical"` for horizontal bars (D-06)

Config:
- `margin={{ top: 8, right: 48, left: 8, bottom: 8 }}`
- `YAxis dataKey="label"` — 14px, color muted, max 28 characters, truncated with ellipsis via `tickFormatter`
- `XAxis domain={[0, 100]}` — hidden tick line, shows 0% and 100% only
- `Bar dataKey="hitRate"` — `Cell` fill driven by performance threshold per bar
- `Tooltip` — dark surface background (`var(--surface-2)`), 1px border `var(--border)`, 12px text, showing KPI name + "NN% hit rate"
- No legend (labels are on the Y axis)
- Right-side percentage annotation: recharts `LabelList` position="right", 12px, weight 700, color `var(--text)`

Miss streak badge (D-10): Rendered as a custom `Bar` label or as an absolutely-positioned element after the chart row for each KPI with 2+ consecutive misses. Text: "missed N weeks" — 12px, weight 400, color `var(--muted-2)`. No additional color escalation.

Wrapper class: `.progress-chart` — `background: var(--surface); border: 1px solid var(--border); border-radius: 16px; padding: 24px; margin-bottom: 24px`

Section heading: `<h3 className="progress-section-heading">KPI Performance</h3>` — 20px, weight 700, margin-bottom 16px

### 5. Growth Priority Status Cards (progress page, D-15)

Section heading: `<h3 className="progress-section-heading">Growth Priorities</h3>` — 20px, weight 700

Layout: flex column, gap 12px

Each card class: `.progress-growth-card` — `background: var(--surface); border: 1px solid var(--border); border-radius: 12px; padding: 16px 20px`

Card internals:
- Group label (e.g. "PERSONAL GROWTH" or "BUSINESS GROWTH 1 of 2"): 12px, weight 700, uppercase, letter-spacing 0.18em, color `var(--gold)` — reuses existing growth-priority-group-label pattern
- Priority name: 15px, weight 400, color `var(--text)`
- Status badge: `<span className="progress-growth-status-badge progress-growth-status--[status]">` — 12px weight 700 uppercase. Status values: active (muted), achieved (success green), stalled (miss red), deferred (muted-2)
- Trace's note (if any): `<div className="progress-growth-note"><span className="progress-growth-note-label">TRACE'S NOTE</span><p>[note text]</p></div>` — label 11px gold uppercase; note text 14px muted, line-height 1.6
- "Last updated" timestamp: omitted (per Claude's Discretion — keep it simple for partners)
- Read-only: no edit controls visible to partners

### 6. Empty State (no scorecards yet)

Shown on progress page when partner has locked KPIs but zero completed scorecard weeks.

Class: `.progress-empty` — centered, padding 48px 0

Elements:
- Eyebrow: `<span className="eyebrow">NO DATA YET</span>` — gold
- Heading: `<h3>Season tracking starts after your first check-in</h3>` — 20px
- Body: `<p>Complete your first weekly scorecard to see your hit rate and KPI trends here.</p>` — 15px muted

Hub card when no scorecards: still shows card with hit-rate "—" and week "Week N of ~26", no sparkline bars rendered.

### 7. Mock Components (D-16)

PartnerProgressMock.jsx mirrors MeetingSummaryMock.jsx pattern:
- Hardcoded sample data: 3 KPIs (2 mandatory + 1 choice), 6 mock scorecard weeks
- Mock hit-rates: kpi_1 = 83%, kpi_2 = 50%, kpi_3 = 33% — exercises all three color thresholds
- Mock miss streak: kpi_3 has 3 consecutive misses
- Mock growth priority: 1 active personal + 2 business (1 achieved, 1 active) with sample Trace notes
- Route entry via AdminTest.jsx alongside existing mock links

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Hub card title | Season Overview |
| Hub card description | Your cumulative KPI hit rate, weekly trends, and growth priority status for Spring Season 2026. |
| Hub card CTA | View season progress → |
| Hub card hit-rate (loaded) | [NN]% this season |
| Hub card week indicator | Week N of ~26 |
| Hub card worst streak (conditional) | [KPI name]: missed N weeks |
| Hub card empty (no scorecards) | — this season (dash placeholder, no percentage) |
| Progress page eyebrow | SPRING SEASON 2026 |
| Progress page stat label | season hit rate |
| Progress page week label | Week N of ~26 |
| Progress page chart section heading | KPI Performance |
| Progress page growth section heading | Growth Priorities |
| Miss streak badge | missed N weeks |
| Growth priority Trace note label | TRACE'S NOTE |
| Growth status: active | Active |
| Growth status: achieved | Achieved |
| Growth status: stalled | Stalled |
| Growth status: deferred | Deferred |
| Empty state heading | Season tracking starts after your first check-in |
| Empty state body | Complete your first weekly scorecard to see your hit rate and KPI trends here. |
| Load error (progress page) | Couldn't load season data. Refresh and try again. |
| Back nav | ← Back to Hub |
| Mock eyebrow qualifier | MOCK DATA |

No destructive actions in this phase. No destructive confirmation copy needed.

---

## Interaction Contracts

### Hub Card Navigation
- Clicking the Season Overview hub card navigates to `/progress/:partner` (Link component)
- Card is a Link element, not a button — same pattern as Role Definition and Meeting History cards
- No loading state on the card itself; data fetches on the progress page mount

### Progress Page Load Sequence
1. Mount: show `null` (matches Scorecard.jsx early-return pattern) while data fetches
2. On success: animate in with `motionProps` (opacity 0→1, y 8→0, 0.28s easeOut)
3. On error: show inline error paragraph below back nav — no full-page error state

### Bar Chart Interactions
- Tooltip appears on recharts default hover — dark themed to match `var(--surface-2)` background
- No click interactions on bars
- No zoom or scroll on the chart
- Chart width: 100% of container (responsive via recharts `<ResponsiveContainer width="100%">`)
- Chart height: `max(240px, kpiCount * 48px)` — computed inline, minimum 240px

### Week Progress Calculation
- "Week N" = number of Monday anchors from season start date to current Monday inclusive
- Season start: 2026-03-30 (first Monday of Spring Season 2026, per CURRENT_SEASON reference)
- "~26" is a fixed display constant (standard business season length)
- Computed via `getMondayOf()` from lib/week.js

### Hit-Rate Calculation
- Season hit-rate: `hits / possible` where `result !== null` rows only (per STATE.md null-exclusion decision)
- Per-KPI hit-rate: same null-exclusion rule applied per KPI individually
- Miss streak: consecutive `result === 'no'` from most-recent week backward; resets on any `result === 'yes'` or `result === null`

---

## CSS Classes — New Declarations Required

All new classes follow existing kebab-case convention. Add to `src/index.css` at bottom of file, grouped under `/* --- Season Overview & Progress (Phase 11) --- */` comment.

| Class | Description |
|-------|-------------|
| `.progress-hit-rate` | Hub card hit-rate span — 15px, weight 700, color driven by JS inline style |
| `.progress-week-label` | Hub card week indicator — 12px, weight 400, muted |
| `.progress-streak-alert` | Hub card worst streak line — 12px, weight 400, muted-2 |
| `.progress-sparklines` | Hub card sparkline container — flex row, gap 4px, align-items flex-end |
| `.progress-sparkline-bar` | Individual mini bar — height 6px, border-radius 3px, width and background set inline |
| `.progress-overview` | Progress page overview card — surface bg, border, border-radius 16px, padding 24px |
| `.progress-stat-display` | Flex row wrapping the large % + label — align-items baseline, gap 8px |
| `.progress-stat-value` | 28px weight 700 display number |
| `.progress-stat-label` | 12px weight 400 muted label beside stat value |
| `.progress-week-indicator` | 15px weight 400 week progress string |
| `.progress-chart` | recharts wrapper card — surface bg, border, border-radius 16px, padding 24px |
| `.progress-section-heading` | Section h3 — 20px weight 700, margin-bottom 16px |
| `.progress-growth-card` | Growth priority card — surface bg, border, border-radius 12px, padding 16px 20px |
| `.progress-growth-status-badge` | Status badge base — 12px weight 700 uppercase |
| `.progress-growth-status--active` | Color: var(--muted) |
| `.progress-growth-status--achieved` | Color: var(--success) |
| `.progress-growth-status--stalled` | Color: var(--miss) |
| `.progress-growth-status--deferred` | Color: var(--muted-2) |
| `.progress-growth-note` | Trace note container — margin-top 8px, border-top 1px solid var(--border), padding-top 8px |
| `.progress-growth-note-label` | "TRACE'S NOTE" label — 11px gold uppercase letter-spacing 0.18em |
| `.progress-empty` | Empty state container — padding 48px 0, text-align center |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| third-party | none | not applicable |

recharts 3.8.1 is a pre-approved npm dependency (STATE.md decision). No new registry components.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
