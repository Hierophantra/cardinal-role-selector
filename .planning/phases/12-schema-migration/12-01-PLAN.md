---
phase: 12-schema-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/008_schema_v13.sql
autonomous: true
requirements:
  - SCHM-01
  - SCHM-02

must_haves:
  truths:
    - "Monday Prep stop keys (clear_the_air, week_preview, priorities_focus, risks_blockers, growth_checkin, commitments) are accepted by the meeting_notes CHECK constraint"
    - "Friday Review's clear_the_air key is accepted by the CHECK constraint"
    - "All 12 existing stop keys remain valid after migration"
    - "Running the migration twice does not error (idempotent)"
  artifacts:
    - path: "supabase/migrations/008_schema_v13.sql"
      provides: "CHECK constraint expansion for v1.3 stop keys"
      contains: "meeting_notes_stop_key_check"
  key_links:
    - from: "supabase/migrations/008_schema_v13.sql"
      to: "meeting_notes table"
      via: "ALTER TABLE DROP/ADD CONSTRAINT"
      pattern: "DROP CONSTRAINT IF EXISTS meeting_notes_stop_key_check"
---

<objective>
Create migration 008 that expands the meeting_notes CHECK constraint to accept 6 new Monday Prep stop keys and Friday Review's new clear_the_air key.

Purpose: Gate for Phases 13-14 — the database must accept new stop keys before any UI work can reference them.
Output: `supabase/migrations/008_schema_v13.sql` ready to run against Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-schema-migration/12-CONTEXT.md

<interfaces>
<!-- Existing constraint pattern from migration 006, Section 5 -->
<!-- This is the exact pattern to replicate with an expanded key list -->

From supabase/migrations/006_schema_v11.sql (lines 61-66):
```sql
ALTER TABLE meeting_notes DROP CONSTRAINT IF EXISTS meeting_notes_stop_key_check;
ALTER TABLE meeting_notes ADD CONSTRAINT meeting_notes_stop_key_check
  CHECK (agenda_stop_key IN (
    'intro', 'kpi_1', 'kpi_2', 'kpi_3', 'kpi_4', 'kpi_5', 'kpi_6', 'kpi_7',
    'growth_personal', 'growth_business_1', 'growth_business_2', 'wrap'
  ));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 008_schema_v13.sql with expanded CHECK constraint</name>
  <files>supabase/migrations/008_schema_v13.sql</files>
  <read_first>
    supabase/migrations/006_schema_v11.sql
    supabase/migrations/005_admin_meeting_phase4.sql
    .planning/phases/12-schema-migration/12-CONTEXT.md
  </read_first>
  <action>
Create file `supabase/migrations/008_schema_v13.sql` with the following exact content:

1. **Header comment block** (per D-04):
   - Migration name: `008_schema_v13.sql`
   - Phase: `Phase 12: Schema Migration (v1.3 Monday Prep Redesign)`
   - Purpose: Expands meeting_notes CHECK constraint to accept 6 new Monday Prep stop keys and Friday Review's clear_the_air key.
   - Note: No data migration — all 12 existing keys preserved.

2. **Section 1: Expand meeting_notes CHECK constraint** (per D-03, D-05, D-06):
   - `ALTER TABLE meeting_notes DROP CONSTRAINT IF EXISTS meeting_notes_stop_key_check;` (idempotent per D-05)
   - `ALTER TABLE meeting_notes ADD CONSTRAINT meeting_notes_stop_key_check CHECK (agenda_stop_key IN (...));`
   - The IN list must contain exactly these 17 keys, organized by group for readability:
     - Existing Friday Review keys (12): `'intro'`, `'kpi_1'`, `'kpi_2'`, `'kpi_3'`, `'kpi_4'`, `'kpi_5'`, `'kpi_6'`, `'kpi_7'`, `'growth_personal'`, `'growth_business_1'`, `'growth_business_2'`, `'wrap'`
     - Shared key (1): `'clear_the_air'` (per D-02, used by both meeting types)
     - New Monday Prep-only keys (4): `'week_preview'`, `'priorities_focus'`, `'risks_blockers'`, `'growth_checkin'`, `'commitments'`
   - Use SQL comments to label each group within the IN list (e.g., `-- Existing Friday Review stops (12)`, `-- Shared (1)`, `-- New Monday Prep stops (5)`)

3. **Footer comment**: `-- END OF MIGRATION 008`

The file must contain NO other statements — no DELETE, no INSERT, no ALTER on other tables. This is purely a constraint expansion.
  </action>
  <verify>
    <automated>grep -c "DROP CONSTRAINT IF EXISTS meeting_notes_stop_key_check" supabase/migrations/008_schema_v13.sql && grep -c "ADD CONSTRAINT meeting_notes_stop_key_check" supabase/migrations/008_schema_v13.sql && grep -c "clear_the_air" supabase/migrations/008_schema_v13.sql && grep -c "week_preview" supabase/migrations/008_schema_v13.sql && grep -c "priorities_focus" supabase/migrations/008_schema_v13.sql && grep -c "risks_blockers" supabase/migrations/008_schema_v13.sql && grep -c "growth_checkin" supabase/migrations/008_schema_v13.sql && grep -c "commitments" supabase/migrations/008_schema_v13.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/008_schema_v13.sql` exists
    - File contains `DROP CONSTRAINT IF EXISTS meeting_notes_stop_key_check` (idempotent drop)
    - File contains `ADD CONSTRAINT meeting_notes_stop_key_check` (constraint creation)
    - File contains all 12 existing keys: `intro`, `kpi_1` through `kpi_7`, `growth_personal`, `growth_business_1`, `growth_business_2`, `wrap`
    - File contains shared key: `clear_the_air`
    - File contains 5 new Monday keys: `week_preview`, `priorities_focus`, `risks_blockers`, `growth_checkin`, `commitments`
    - File contains NO `DELETE FROM` statements
    - File contains NO `INSERT INTO` statements
    - File contains NO `ALTER TABLE` statements other than the two for meeting_notes constraint
    - Total unique keys in the IN list = 17
  </acceptance_criteria>
  <done>Migration file 008_schema_v13.sql contains the idempotent DROP+ADD constraint with all 17 stop keys. No data modification statements present. File follows the established migration comment style.</done>
</task>

</tasks>

<verification>
1. File exists at `supabase/migrations/008_schema_v13.sql`
2. All 17 keys present in the CHECK constraint IN list
3. No destructive statements (DELETE, INSERT, DROP TABLE)
4. DROP CONSTRAINT IF EXISTS ensures idempotency (SCHM-02)
5. All 12 original keys preserved for backward compatibility (SCHM-02)
6. New keys match D-01 snake_case naming: clear_the_air, week_preview, priorities_focus, risks_blockers, growth_checkin, commitments
</verification>

<success_criteria>
- Migration file 008_schema_v13.sql is committed and ready to deploy
- CHECK constraint will accept all 6 Monday Prep stop keys + clear_the_air for Friday Review
- Existing meeting data unaffected (no data statements in migration)
- Migration is idempotent (can run multiple times without error)
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-migration/12-01-SUMMARY.md`
</output>
