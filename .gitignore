node_modules
dist
.env
.env.local
.env.*.local
.vite
.DS_Store
*.log

# Claude Code local config + ephemeral agent worktrees (never commit)
.claude/

# GSD2 working directory (compromised tool — never reintroduce)
.gsd/
